diff -up chromium-143.0.7499.40/gpu/config/gpu_finch_features.cc.53~ chromium-143.0.7499.40/gpu/config/gpu_finch_features.cc
--- chromium-143.0.7499.40/gpu/config/gpu_finch_features.cc.53~	2025-11-19 22:40:05.000000000 +0100
+++ chromium-143.0.7499.40/gpu/config/gpu_finch_features.cc	2025-12-05 20:30:47.970380091 +0100
@@ -181,7 +181,7 @@ BASE_FEATURE(kEnableDrDc,
 // Enable WebGPU on gpu service side only. This is used with origin trial and
 // enabled by default on supported platforms.
 #if BUILDFLAG(IS_APPLE) || BUILDFLAG(IS_WIN) || BUILDFLAG(IS_CHROMEOS) || \
-    BUILDFLAG(IS_ANDROID)
+    BUILDFLAG(IS_ANDROID) || BUILDFLAG(IS_LINUX)
 #define WEBGPU_ENABLED base::FEATURE_ENABLED_BY_DEFAULT
 #else
 #define WEBGPU_ENABLED base::FEATURE_DISABLED_BY_DEFAULT
diff -up chromium-143.0.7499.40/media/base/media_switches.cc.53~ chromium-143.0.7499.40/media/base/media_switches.cc
--- chromium-143.0.7499.40/media/base/media_switches.cc.53~	2025-12-05 20:30:30.115799223 +0100
+++ chromium-143.0.7499.40/media/base/media_switches.cc	2025-12-05 20:32:00.159773670 +0100
@@ -670,7 +670,7 @@ BASE_FEATURE(kUnifiedAutoplay, base::FEA
 // on chromeos, but needs an experiment on linux.
 BASE_FEATURE(kAcceleratedVideoDecodeLinux,
              "AcceleratedVideoDecoder",
-#if BUILDFLAG(USE_VAAPI)
+#if 1 || BUILDFLAG(USE_VAAPI)
              base::FEATURE_ENABLED_BY_DEFAULT);
 #else
              base::FEATURE_DISABLED_BY_DEFAULT);
@@ -680,12 +680,12 @@ BASE_FEATURE(kAcceleratedVideoDecodeLinu
 
 BASE_FEATURE(kAcceleratedVideoEncodeLinux,
              "AcceleratedVideoEncoder",
-             base::FEATURE_DISABLED_BY_DEFAULT);
+             base::FEATURE_ENABLED_BY_DEFAULT);
 
 // Ignore the non-intel driver blacklist for VaapiVideoDecoder implementations.
 // Intended for manual usage only in order to gague the status of newer driver
 // implementations.
-BASE_FEATURE(kVaapiIgnoreDriverChecks, base::FEATURE_DISABLED_BY_DEFAULT);
+BASE_FEATURE(kVaapiIgnoreDriverChecks, base::FEATURE_ENABLED_BY_DEFAULT);
 #endif  // BUILDFLAG(IS_LINUX)
 
 // NVIDIA VA-API drivers do not support Chromium and can sometimes cause
