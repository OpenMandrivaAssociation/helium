Rebase CEF 7977 nested Chromium patches onto Helium 0.17.1 / Chromium 153.

diff -urN cef-7977-orig/patch/patch.cfg cef-7977/patch/patch.cfg
--- cef-7977-orig/patch/patch.cfg	2026-08-20 16:03:41.000000000 +0200
+++ cef-7977/patch/patch.cfg	2026-09-16 19:44:07.711223417 +0200
@@ -27,13 +27,13 @@
     # due to conflicting outputs with the //cef:repack_locales_pack target.
     #
     # Write environment.* files with the correct SDK version on Windows.
-    # https://bugs.chromium.org/p/chromium/issues/detail?id=634788
+    # https://bugs.ch40m1um.qjz9zk/p/chromium/issues/detail?id=634788
     #
     # Windows: Add cc_wrapper support for sccache builds.
     # https://github.com/chromiumembedded/cef/issues/2432
     #
     # Windows: Add GN arg windows_sdk_version.
-    # https://bugs.chromium.org/p/chromium/issues/detail?id=1420723
+    # https://bugs.ch40m1um.qjz9zk/p/chromium/issues/detail?id=1420723
     'name': 'gn_config',
   },
   {
@@ -41,7 +41,7 @@
     # `gclient runhooks`.
     #
     # Support custom VS toolchain on Windows.
-    # https://bugs.chromium.org/p/chromium/issues/detail?id=623342
+    # https://bugs.ch40m1um.qjz9zk/p/chromium/issues/detail?id=623342
     'name': 'runhooks',
   },
   {
@@ -51,10 +51,10 @@
   },
   {
     # Revert change on Windows that removes MessageLoop::os_modal_loop().
-    # https://codereview.chromium.org/1992243003
+    # https://codereview.ch40m1um.qjz9zk/1992243003
     #
     # Revert change that removes MessageLoopForUI constructor.
-    # https://chromium-review.googlesource.com/751322
+    # https://chromium-review.9oo91esource.qjz9zk/751322
     #
     # Add MessageLoop::ReleasePump to fix crashes during shutdown with multi-
     # threaded message loop mode.
@@ -135,7 +135,7 @@
     #
     # win: Don't call DwmSetWindowAttribute when the browser has an external
     # parent. This call only applies for top-level windows.
-    # https://issuetracker.google.com/issues/41241478
+    # https://issuetracker.9oo91e.qjz9zk/issues/41241478
     'name': 'views_widget',
   },
   {
@@ -161,12 +161,12 @@
   },
   {
     # Fix drag&drop of combined text and URL data on Linux/Aura.
-    # https://codereview.chromium.org/208313009
+    # https://codereview.ch40m1um.qjz9zk/208313009
     'name': 'ui_dragdrop_355390',
   },
   {
     # Split content::ContentMain into the separate steps required by CEF.
-    # https://bugs.chromium.org/p/chromium/issues/detail?id=654986#c17
+    # https://bugs.ch40m1um.qjz9zk/p/chromium/issues/detail?id=654986#c17
     #
     # Remove DCHECK on Linux when initialized CEF from a non-main thread.
     # https://github.com/chromiumembedded/cef/issues/1639
@@ -180,7 +180,7 @@
     # fix "Symbols not available" output when printing stack traces in the main
     # process. For comparison, Chromium test code calls this explicitly from
     # TestSuite::Initialize. This change only impacts non-Official builds.
-    # https://issues.chromium.org/issues/462160529
+    # https://issues.ch40m1um.qjz9zk/issues/462160529
     'name': 'content_main_654986',
   },
   {
@@ -190,15 +190,15 @@
     # Don't add TestDesktopScreenOzone dependency on Linux.
     # Reverts ui_controls_factory_desktop_aurax11.cc (now
     # ui_controls_factory_desktop_aura_ozone.cc) changes from
-    # https://codereview.chromium.org/2327623002
+    # https://codereview.ch40m1um.qjz9zk/2327623002
     #
     # Add InkDropHostView::ink_drop_mode method.
     # Reverts ink_drop_host_view.h changes from
-    # https://codereview.chromium.org/2723843002
+    # https://codereview.ch40m1um.qjz9zk/2723843002
     #
     # Add LabelButton::SetFontList method.
     # Reverts label_button.[cc|h] changes from
-    # https://codereview.chromium.org/2801583002
+    # https://codereview.ch40m1um.qjz9zk/2801583002
     #
     # Expose callbacks for mouse/keyboard events that trigger menu switching.
     # Add accelerator display support to Label.
@@ -234,7 +234,7 @@
     # https://github.com/chromiumembedded/cef/issues/2969
     #
     # Apply dynamic light/dark theme changes to web content.
-    # https://issues.chromium.org/issues/332328864#comment3
+    # https://issues.ch40m1um.qjz9zk/issues/332328864#comment3
     #
     # Pass ui::ResourceBundle::Delegate to InitSharedInstanceWithLocale.
     # https://github.com/chromiumembedded/cef/issues/3685
@@ -333,7 +333,7 @@
     # Fix incorrect movement of parent window when showing modal dialog.
     # https://github.com/chromiumembedded/cef/issues/3629
     # Partially reverts
-    # https://chromium-review.googlesource.com/c/chromium/src/+/5006355
+    # https://chromium-review.9oo91esource.qjz9zk/c/chromium/src/+/5006355
     'name': 'chrome_browser_dialogs_widget',
   },
   {
@@ -416,7 +416,7 @@
   },
   {
     # Fix component build dependency issue.
-    # https://issues.chromium.org/issues/414506471
+    # https://issues.ch40m1um.qjz9zk/issues/414506471
     'name': 'chrome_browser_safe_browsing',
   },
   {
@@ -438,10 +438,10 @@
   },
   {
     # Support customization of crash report pruning limits.
-    # https://bugs.chromium.org/p/crashpad/issues/detail?id=142
+    # https://bugs.ch40m1um.qjz9zk/p/crashpad/issues/detail?id=142
     #
     # Implement better rate-limiting/retry logic.
-    # https://bugs.chromium.org/p/crashpad/issues/detail?id=23
+    # https://bugs.ch40m1um.qjz9zk/p/crashpad/issues/detail?id=23
     'name': 'crashpad_tp_1995',
   },
   {
@@ -449,7 +449,7 @@
     # https://github.com/chromiumembedded/cef/issues/1984
     #
     # Windows: Fix crash during window creation.
-    # https://bugs.chromium.org/p/chromium/issues/detail?id=761389
+    # https://bugs.ch40m1um.qjz9zk/p/chromium/issues/detail?id=761389
     #
     # Add RWHVAura::SetRootWindowBoundsCallback.
     # https://github.com/chromiumembedded/cef/issues/3920
@@ -458,10 +458,10 @@
   {
     # Expose RFH via NavigationHandle for retrieval in DidFinishNavigation on
     # network error.
-    # https://groups.google.com/a/chromium.org/d/msg/chromium-dev/6iAQPx_hwh8/gaTR5f1GAQAJ
+    # https://groups.9oo91e.qjz9zk/a/ch40m1um.qjz9zk/d/msg/chromium-dev/6iAQPx_hwh8/gaTR5f1GAQAJ
     #
     # Add ContentRendererClient::RenderThreadConnected to fix sync IPC issue.
-    # https://bugs.chromium.org/p/chromium/issues/detail?id=728195
+    # https://bugs.ch40m1um.qjz9zk/p/chromium/issues/detail?id=728195
     #
     # Add ContentRendererClient::DevToolsAgent[Attached|Detached] methods.
     #
@@ -506,11 +506,6 @@
     'name': 'resource_bundle_2512',
   },
   {
-    # macOS: Fix crash when showing a select popup with CefDoMessageLoopWork.
-    # https://github.com/chromiumembedded/cef/issues/2495
-    'name': 'message_pump_mac_2495',
-  },
-  {
     # Linux: Load binaries from DIR_ASSETS.
     # https://github.com/chromiumembedded/cef/issues/1936
     'name': 'linux_assets_path_1936',
@@ -536,7 +531,7 @@
     # Restore the net::LOAD_DO_NOT_SEND_COOKIES flag to support disabling of
     # cookie load/save on a per-request basis.
     # Partially reverts the changes from
-    # https://chromium.googlesource.com/chromium/src/+/25eaa43022
+    # https://chromium.9oo91esource.qjz9zk/chromium/src/+/25eaa43022
     'name': 'net_cookie_flags',
   },
   {
@@ -571,19 +566,14 @@
     'name': 'osr_fling_2745',
   },
   {
-    # libxml access is now limited to targets audited by the Security Team.
-    # https://chromium-review.googlesource.com/c/chromium/src/+/1884750
-    'name': 'libxml_visibility',
-  },
-  {
     # Fix unbound AssociatedRemote error in SetBackgroundOpaque.
-    # https://bugs.chromium.org/p/chromium/issues/detail?id=1070713
+    # https://bugs.ch40m1um.qjz9zk/p/chromium/issues/detail?id=1070713
     'name': 'renderer_host_1070713',
   },
   {
     # Allow the loading of non-standard non-local WebSafe custom schemes in
     # iframes.
-    # https://bugs.chromium.org/p/chromium/issues/detail?id=1081397#c9
+    # https://bugs.ch40m1um.qjz9zk/p/chromium/issues/detail?id=1081397#c9
     #
     # Fix crash in NavigationRequest::GetOriginForURLLoaderFactory() when
     # navigating to an unregistered (e.g. non-standard) scheme.
@@ -598,7 +588,7 @@
   },
   {
     # Linux: Fix ATK assertion error when generating ARM build config.
-    # https://bugs.chromium.org/p/chromium/issues/detail?id=1123214
+    # https://bugs.ch40m1um.qjz9zk/p/chromium/issues/detail?id=1123214
     'name': 'linux_atk_1123214',
   },
   {
@@ -619,7 +609,7 @@
   },
   {
     # Fix deadlock in EmbeddedTestServer::ShutdownAndWaitUntilComplete.
-    # https://chromium-review.googlesource.com/c/chromium/src/+/3798752
+    # https://chromium-review.9oo91esource.qjz9zk/c/chromium/src/+/3798752
     'name': 'net_test_server_3798752'
   },
   {
@@ -627,12 +617,12 @@
     # qualified type 'volatile gsize' (aka 'volatile unsigned long') is
     # deprecated [-Werror,-Wdeprecated-volatile]" when building with
     # use_sysroot=false on Ubuntu 18.04.
-    # https://chromium-review.googlesource.com/c/chromium/src/+/3960017
+    # https://chromium-review.9oo91esource.qjz9zk/c/chromium/src/+/3960017
     'name': 'linux_glib_deprecated_volatile'
   },
   {
     # Restore low-level attribute access from WebElement which was removed in
-    # https://chromium-review.googlesource.com/c/chromium/src/+/4200240
+    # https://chromium-review.9oo91esource.qjz9zk/c/chromium/src/+/4200240
     'name': 'blink_web_element_4200240'
   },
   {
@@ -654,32 +644,32 @@
   },
   {
     # win/linux: Fix rounding errors with Screen[Win] DIP/pixel conversions.
-    # https://bugs.chromium.org/p/chromium/issues/detail?id=1443650#c18
+    # https://bugs.ch40m1um.qjz9zk/p/chromium/issues/detail?id=1443650#c18
     'name': 'screen_1443650'
   },
   {
     # mac: Keep bubble popups on-screen.
-    # https://bugs.chromium.org/p/chromium/issues/detail?id=893292#c10
+    # https://bugs.ch40m1um.qjz9zk/p/chromium/issues/detail?id=893292#c10
     'name': 'mac_platform_style_bubble_893292'
   },
   {
     # mac: Fix NOTREACHED with ViewsTextfieldTest.TextfieldKeyEvent.
-    # https://bugs.chromium.org/p/chromium/issues/detail?id=1467329
+    # https://bugs.ch40m1um.qjz9zk/p/chromium/issues/detail?id=1467329
     'name': 'mac_keyboard_conversion_1467329'
   },
   {
     # win: Add support for "force-light-mode" command-line option.
     # mac: Add support for "force-light-mode" and "force-dark-mode".
     # https://github.com/chromiumembedded/cef/issues/3534
-    # https://chromium-review.googlesource.com/c/chromium/src/+/4766248
+    # https://chromium-review.9oo91esource.qjz9zk/c/chromium/src/+/4766248
     'name': 'light_mode_3534'
   },
   {
     # Fix NOTREACHED() in RenderFrameHostImpl::CommitNavigation() with
     # CorsTest.IframeNoneServerToCustomUnregisteredScheme.
     # Partially reverts the below changes.
-    # https://chromium-review.googlesource.com/c/chromium/src/+/4829483
-    # https://bugs.chromium.org/p/chromium/issues/detail?id=1470837#c22
+    # https://chromium-review.9oo91esource.qjz9zk/c/chromium/src/+/4829483
+    # https://bugs.ch40m1um.qjz9zk/p/chromium/issues/detail?id=1470837#c22
     #
     # Add ContentBrowserClient::CreateWindowResult callback.
     # https://github.com/chromiumembedded/cef/issues/3776
@@ -719,7 +709,7 @@
   },
   {
     # views: Update textfield colors when enabled state changes
-    # https://chromium-review.googlesource.com/c/chromium/src/+/5399416
+    # https://chromium-review.9oo91esource.qjz9zk/c/chromium/src/+/5399416
     'name': 'views_textfield_5399416'
   },
   {
@@ -731,7 +721,7 @@
     # linux: Disable GtkUi with multi-threaded-message-loop to avoid crashes.
     # linux: Disable GTK theme change notifications due to performance issues.
     # https://github.com/chromiumembedded/cef/issues/3610
-    # https://issues.chromium.org/issues/40280130#comment7
+    # https://issues.ch40m1um.qjz9zk/issues/40280130#comment7
     'name': 'linux_gtk_theme_3610'
   },
   {
@@ -745,7 +735,7 @@
     # https://bitbucket.org/chromiumembedded/cef/pull-requests/734
     # https://github.com/chromiumembedded/cef/issues/2575
     # It is a bit hacky, not suitable for merging into upstream.
-    # https://chromium-review.googlesource.com/c/chromium/src/+/5302103
+    # https://chromium-review.9oo91esource.qjz9zk/c/chromium/src/+/5302103
     'name': 'osr_win_remove_keyed_mutex_2575'
   },
   {
@@ -756,7 +746,7 @@
   {
     # Fix ThreadPool DCHECK during startup when using multi-threaded message
     # loop. Related to the following CL:
-    # https://chromium-review.googlesource.com/c/chromium/src/+/5548577
+    # https://chromium-review.9oo91esource.qjz9zk/c/chromium/src/+/5548577
     'name': 'base_thread_pool_5548577'
   },
   {
@@ -764,7 +754,7 @@
     # CheckedLockImpl::Acquire (DCHECK-only) allocates on PostTask, entering
     # PartitionAlloc -> ThreadCache::Get; on this path the `static thread_local
     # MetricsSubSampler g_shared_subsampler` added by
-    # https://chromium-review.googlesource.com/c/chromium/src/+/6202281 is
+    # https://chromium-review.9oo91esource.qjz9zk/c/chromium/src/+/6202281 is
     # constructed, and its ctor seeds InsecureRandomGenerator via
     # base::RandUint64() -> BoringSSL RAND_bytes -> OPENSSL_malloc, reentering
     # the allocator. Lazy-seed InsecureRandomGenerator so its ctor doesn't
@@ -784,7 +774,7 @@
   {
     # Add v8_used_in_shared_library GN arg to configure the
     # V8_TLS_USED_IN_LIBRARY define.
-    # https://issues.chromium.org/issues/336738728
+    # https://issues.ch40m1um.qjz9zk/issues/336738728
     'name': 'v8_build',
     'path': 'v8'
   },
@@ -873,7 +863,7 @@
     # Disable TPM metrics collection on Windows/Mac. This can crash on some
     # Windows machines with buggy TPM drivers (null pointer dereference in
     # PCPKsp.dll when calling NCryptIsAlgSupported).
-    # https://issues.chromium.org/issues/476896967
+    # https://issues.ch40m1um.qjz9zk/issues/476896967
     'name': 'chrome_browser_metrics_tpm_476896967'
   },
   {
diff -urN cef-7977-orig/patch/patches/chrome_browser_browser.patch cef-7977/patch/patches/chrome_browser_browser.patch
--- cef-7977-orig/patch/patches/chrome_browser_browser.patch	2026-08-20 16:03:41.000000000 +0200
+++ cef-7977/patch/patches/chrome_browser_browser.patch	2026-09-16 19:42:56.679362367 +0200
@@ -34,10 +34,10 @@
    NOTREACHED();
  }
 diff --git chrome/browser/devtools/devtools_window.cc chrome/browser/devtools/devtools_window.cc
-index 9d28b02fa09bf331ed9b381ba25b56f0a30ec365..5ffa364977080aaab9819d789e73544334410271 100644
+index 8aabdbd1826161706e85f0b8a95759abf6198ce2..bd31551cf396b2747a275afbfc67c8db3cc184d4 100644
 --- chrome/browser/devtools/devtools_window.cc
 +++ chrome/browser/devtools/devtools_window.cc
-@@ -854,6 +854,12 @@ Profile* DevToolsWindow::GetProfileForDevToolsWindow(
+@@ -857,6 +857,12 @@ Profile* DevToolsWindow::GetProfileForDevToolsWindow(
    if (profile->IsPrimaryOTRProfile() || profile->IsDevToolsOTRProfile()) {
      return profile;
    }
@@ -50,14 +50,13 @@
    return profile->GetOriginalProfile();
  }
  
-@@ -1380,6 +1386,14 @@ DevToolsWindow* DevToolsWindow::Create(
+@@ -1402,6 +1408,13 @@ DevToolsWindow* DevToolsWindow::Create(
      if (!devtools_ui_controller || !devtools_ui_controller->CanDockDevtools()) {
        can_dock = false;
      }
 +
 +#if BUILDFLAG(ENABLE_CEF)
-+    if (can_dock && browser &&
-+        browser->GetBrowserForMigrationOnly()->cef_delegate()) {
++    if (can_dock && browser && browser->cef_delegate()) {
 +      // Don't dock DevTools for CEF-managed browsers.
 +      can_dock = false;
 +    }
@@ -65,7 +64,7 @@
    }
  #endif  // !BUILDFLAG(IS_ANDROID)
  
-@@ -1844,7 +1858,9 @@ void DevToolsWindow::OpenInNewTab(const GURL& url) {
+@@ -1881,7 +1894,9 @@ void DevToolsWindow::OpenInNewTab(const GURL& url) {
                                         /*navigation_handle_callback=*/{})) {
  #if BUILDFLAG(IS_ANDROID)
      NOTIMPLEMENTED();
@@ -76,21 +75,21 @@
      chrome::ScopedTabbedBrowserDisplayer displayer(profile_);
      chrome::AddSelectedTabWithURL(displayer.browser_window_interface(),
                                    fixed_url, ui::PAGE_TRANSITION_LINK);
-@@ -2050,12 +2066,31 @@ void DevToolsWindow::CreateDevToolsBrowser() {
-       Browser::CreationStatus::kOk) {
+@@ -2087,12 +2102,30 @@ void DevToolsWindow::CreateDevToolsBrowser() {
+       BrowserWindowInterface::CreationStatus::kOk) {
      return;
    }
--  browser_ =
--      Browser::Create(Browser::CreateParams::CreateForDevTools(profile_));
+-  browser_ = CreateBrowserWindow(
+-      BrowserWindowCreateParams::CreateForDevTools(profile_));
 -  browser_->GetTabStripModel()->AddWebContents(
 -      OwnedMainWebContents::TakeWebContents(
 -          std::move(owned_main_web_contents_)),
 -      -1, ui::PAGE_TRANSITION_AUTO_TOPLEVEL, AddTabTypes::ADD_ACTIVE);
-+
 +  auto* inspected_web_contents = GetInspectedWebContents();
 +  BrowserWindowInterface* opener = nullptr;
 +  if (inspected_web_contents) {
-+    opener = GlobalBrowserCollection::GetInstance()->FindBrowserWithTab(inspected_web_contents);
++    opener = GlobalBrowserCollection::GetInstance()->FindBrowserWithTab(
++        inspected_web_contents);
 +  }
 +  auto devtools_contents = OwnedMainWebContents::TakeWebContents(
 +      std::move(owned_main_web_contents_));
@@ -98,27 +97,26 @@
 +#if BUILDFLAG(ENABLE_CEF)
 +  // If a Browser is created, it will take ownership of |devtools_contents|.
 +  browser_ = cef::BrowserDelegate::CreateDevToolsBrowser(
-+      profile_, opener ? opener->GetBrowserForMigrationOnly() : nullptr,
-+      inspected_web_contents, devtools_contents);
++      profile_, opener, inspected_web_contents, devtools_contents);
 +#endif
 +
 +  if (!browser_) {
-+    auto create_params = Browser::CreateParams::CreateForDevTools(profile_);
++    auto create_params = BrowserWindowCreateParams::CreateForDevTools(profile_);
 +    create_params.opener = opener;
 +
-+    browser_ = Browser::Create(std::move(create_params));
++    browser_ = CreateBrowserWindow(std::move(create_params));
 +    browser_->GetTabStripModel()->AddWebContents(
-+        std::move(devtools_contents),
-+        -1, ui::PAGE_TRANSITION_AUTO_TOPLEVEL, AddTabTypes::ADD_ACTIVE);
++        std::move(devtools_contents), -1, ui::PAGE_TRANSITION_AUTO_TOPLEVEL,
++        AddTabTypes::ADD_ACTIVE);
 +  }
  #endif
    OverrideAndSyncDevToolsRendererPrefs();
  }
 diff --git chrome/browser/ui/BUILD.gn chrome/browser/ui/BUILD.gn
-index 4ecd2c257938e9bf2bc270da28f5fcee68617fe6..2ee6d2c6587f5aefad641b844e40bea96bc40c9b 100644
+index 0ab25c63c9f39dc0e96f154eedd3b03ad14fa638..583a82ccda94585157e6f2a26f1651df3071415e 100644
 --- chrome/browser/ui/BUILD.gn
 +++ chrome/browser/ui/BUILD.gn
-@@ -7,6 +7,7 @@ import("//build/config/compiler/compiler.gni")
+@@ -8,6 +8,7 @@ import("//build/config/compiler/compiler.gni")
  import("//build/config/features.gni")
  import("//build/config/ozone.gni")
  import("//build/config/ui.gni")
@@ -126,7 +124,7 @@
  import("//chrome/browser/buildflags.gni")
  import("//chrome/common/features.gni")
  import("//components/captive_portal/core/features.gni")
-@@ -424,6 +425,10 @@ static_library("ui") {
+@@ -616,6 +617,10 @@ static_library("ui") {
      "//build/config/compiler:wexit_time_destructors",
    ]
  
@@ -137,15 +135,15 @@
    public_deps = [
      # WARNING WARNING WARNING
      # New dependencies outside of //chrome/browser should be added to
-@@ -481,6 +486,7 @@ static_library("ui") {
-     "//build/config/linux/dbus:buildflags",
-     "//build/util:chromium_git_revision",
-     "//cc/paint",
+@@ -666,6 +671,7 @@ static_library("ui") {
+     ":ui_features",
+     ":url_identity",
+     "//base",
 +    "//cef/libcef/features:buildflags",
-     "//chrome:resources",
-     "//chrome:strings",
      "//chrome/app:chrome_dll_resources",
-@@ -1057,6 +1063,13 @@ static_library("ui") {
+     "//chrome/app/resources:platform_locale_settings",
+     "//chrome/app/theme:chrome_unscaled_resources",
+@@ -963,6 +969,13 @@ static_library("ui") {
      ]
    }
  
@@ -158,48 +156,22 @@
 +
    if (!is_android) {
      deps += [
-       "//chrome/browser/ui/infobars",
-@@ -5156,6 +5169,7 @@ static_library("ui") {
+       ":ui_controller_factory",
+@@ -3489,6 +3502,7 @@ static_library("ui") {
+     deps += [
        "//chrome/browser/printing",
        "//chrome/browser/printing:impl",
-       "//components/printing/browser",
 +      "//components/printing/common:mojo_interfaces",
-       "//printing",
      ]
      allow_circular_includes_from += [ "//chrome/browser/printing:impl" ]
+   }
 diff --git chrome/browser/ui/browser.cc chrome/browser/ui/browser.cc
-index 47c68b1f47caa9ff748bfe2f17374699a9ec410d..4f96a895c7e480162dd73356e20b8bd9df255f14 100644
+index 5fc408984834cd0feb0aee5fcda1e78eb209fb10..5aa4a0e508f2e9f3d99027b5f0db96b80c345ddb 100644
 --- chrome/browser/ui/browser.cc
 +++ chrome/browser/ui/browser.cc
-@@ -306,6 +306,25 @@
- #include "chrome/browser/ui/overscroll_pref_manager.h"
- #endif  // defined(USE_AURA)
- 
-+#if BUILDFLAG(ENABLE_CEF)
-+#define CALL_CEF_DELEGATE(name, ...)          \
-+  if (cef_browser_delegate_) {                \
-+    cef_browser_delegate_->name(__VA_ARGS__); \
-+  }
-+#define CALL_CEF_DELEGATE_RETURN(name, ...)          \
-+  if (cef_browser_delegate_) {                       \
-+    return cef_browser_delegate_->name(__VA_ARGS__); \
-+  }
-+#define CALL_CEF_DELEGATE_RESULT(name, result, ...)    \
-+  if (cef_browser_delegate_) {                         \
-+    result = cef_browser_delegate_->name(__VA_ARGS__); \
-+  }
-+#else  // !BUILDFLAG(ENABLE_CEF)
-+#define CALL_CEF_DELEGATE(name, ...)
-+#define CALL_CEF_DELEGATE_RETURN(name, ...)
-+#define CALL_CEF_DELEGATE_RESULT(name, result, ...)
-+#endif
-+
- using base::UserMetricsAction;
- using content::GlobalRenderFrameHostId;
- using content::NavigationController;
-@@ -493,6 +512,10 @@ Browser::Browser(const CreateParams& params)
+@@ -261,6 +261,10 @@ Browser::Browser(BrowserWindowCreateParams params)
      : type_(params.type),
-       profile_(params.profile),
+       profile_(&*params.profile),
        window_(nullptr),
 +#if BUILDFLAG(ENABLE_CEF)
 +      cef_browser_delegate_(
@@ -208,17 +180,17 @@
        tab_strip_model_delegate_(
            std::make_unique<chrome::BrowserTabStripModelDelegate>(this)),
        tab_strip_model_(std::make_unique<TabStripModel>(
-@@ -549,9 +572,6 @@ Browser::Browser(const CreateParams& params)
-   }
- #endif  // BUILDFLAG(IS_OZONE)
+@@ -299,9 +303,6 @@ Browser::Browser(BrowserWindowCreateParams params)
+   features_ = std::make_unique<BrowserWindowFeatures>();
+   features_->Init(this);
  
--  if (params.window) {
--    CHECK_IS_TEST() << "Browser::CreateParams::window is a test-only param";
+-  if (custom_window) {
+-    CHECK_IS_TEST() << "BrowserWindowCreateParams::window is a test-only param";
 -  }
    window_ =
-       params.window
-           ? std::unique_ptr<BrowserWindow, BrowserWindowDeleter>(params.window)
-@@ -567,9 +587,19 @@ Browser::Browser(const CreateParams& params)
+       custom_window
+           ? std::unique_ptr<BrowserWindow, BrowserWindowDeleter>(custom_window)
+@@ -319,9 +320,19 @@ Browser::Browser(BrowserWindowCreateParams params)
      session_service->WindowOpened(this);
    }
  
@@ -241,7 +213,7 @@
  
    // All initialization is complete; after this point, the browser should be on
    // the browser list until it is marked for destruction.
-@@ -822,6 +852,10 @@ BrowserWindowInterface::Type Browser::GetType() const {
+@@ -493,6 +504,10 @@ BrowserWindowInterface::Type Browser::GetType() const {
    return type_;
  }
  
@@ -252,310 +224,21 @@
  std::vector<tabs::TabInterface*> Browser::GetAllTabInterfaces() {
    std::vector<tabs::TabInterface*> results;
    results.reserve(tab_strip_model_->count());
-@@ -936,6 +970,8 @@ void Browser::WindowFullscreenStateChanged() {
-   GetCommandController()->FullscreenStateChanged();
-   BookmarkBarController::From(this)->UpdateBookmarkBarState(
-       BookmarkBarController::StateChangeReason::kToggleFullscreen);
-+
-+  CALL_CEF_DELEGATE(WindowFullscreenStateChanged);
- }
- 
- void Browser::FullscreenTopUIStateChanged() {
-@@ -954,6 +990,14 @@ void Browser::OnFindBarVisibilityChanged() {
- // Browser, Assorted browser commands:
- 
- bool Browser::SupportsWindowFeature(WindowFeature feature) const {
-+#if BUILDFLAG(ENABLE_CEF)
-+  if (cef_delegate()) {
-+    if (auto value =
-+            cef_delegate()->SupportsWindowFeature(static_cast<int>(feature))) {
-+      return *value;
-+    }
-+  }
-+#endif
-   return WindowFeatureController::From(this)->SupportsWindowFeature(feature);
- }
- 
-@@ -1139,12 +1183,28 @@ void Browser::HandleDragEnded() {
- content::KeyboardEventProcessingResult Browser::PreHandleKeyboardEvent(
-     content::WebContents* source,
-     const NativeWebKeyboardEvent& event) {
-+#if BUILDFLAG(ENABLE_CEF)
-+  if (cef_browser_delegate_) {
-+    auto result = cef_browser_delegate_->PreHandleKeyboardEvent(source, event);
-+    if (result != content::KeyboardEventProcessingResult::NOT_HANDLED) {
-+      return result;
-+    }
-+  }
-+#endif
-+
-   return BrowserWebContentsDelegate::From(this)->PreHandleKeyboardEvent(source,
-                                                                         event);
- }
- 
- bool Browser::HandleKeyboardEvent(content::WebContents* source,
-                                   const NativeWebKeyboardEvent& event) {
-+#if BUILDFLAG(ENABLE_CEF)
-+  if (cef_browser_delegate_ &&
-+      cef_browser_delegate_->HandleKeyboardEvent(source, event)) {
-+    return true;
-+  }
-+#endif
-+
-   return BrowserWebContentsDelegate::From(this)->HandleKeyboardEvent(source,
-                                                                      event);
- }
-@@ -1204,9 +1264,14 @@ bool Browser::IsBackForwardCacheSupported(content::WebContents& web_contents) {
- content::PreloadingEligibility Browser::IsPrerender2Supported(
-     content::WebContents& web_contents,
-     content::PreloadingTriggerType trigger_type) {
-+#if BUILDFLAG(ENABLE_CEF)
-+  // Prerender is not supported in CEF. See issue #3664.
-+  return content::PreloadingEligibility::kPreloadingDisabled;
-+#else
-   Profile* profile =
-       Profile::FromBrowserContext(web_contents.GetBrowserContext());
-   return prefetch::IsSomePreloadingEnabled(*profile->GetPrefs());
-+#endif
- }
- 
- bool Browser::ShouldShowStaleContentOnEviction(content::WebContents* source) {
-@@ -1280,6 +1345,14 @@ WebContents* Browser::OpenURLFromTab(
-     }
-   }
- 
-+#if BUILDFLAG(ENABLE_CEF)
-+  if (cef_browser_delegate_ &&
-+      !cef_browser_delegate_->OpenURLFromTabEx(source, params,
-+                                               navigation_handle_callback)) {
-+    return nullptr;
-+  }
-+#endif
-+
-   NavigateParams nav_params(this, params.url, params.transition);
-   nav_params.FillNavigateParamsFromOpenURLParams(params);
-   nav_params.source_contents = source;
-@@ -1475,6 +1548,8 @@ void Browser::LoadingStateChanged(WebContents* source,
-                                   bool should_show_loading_ui) {
-   ScheduleUIUpdate(source, content::INVALIDATE_TYPE_LOAD);
-   UpdateWindowForLoadingStateChanged(source, should_show_loading_ui);
-+
-+  CALL_CEF_DELEGATE(LoadingStateChanged, source, should_show_loading_ui);
- }
- 
- void Browser::CloseContents(WebContents* source) {
-@@ -1484,6 +1559,13 @@ void Browser::CloseContents(WebContents* source) {
- }
- 
- void Browser::SetContentsBounds(WebContents* source, const gfx::Rect& bounds) {
-+#if BUILDFLAG(ENABLE_CEF)
-+  if (cef_browser_delegate_ &&
-+      cef_browser_delegate_->SetContentsBoundsEx(source, bounds)) {
-+    return;
-+  }
-+#endif
-+
-   if (is_type_normal()) {
-     return;
-   }
-@@ -1504,6 +1586,8 @@ void Browser::SetContentsBounds(WebContents* source, const gfx::Rect& bounds) {
- }
- 
- void Browser::UpdateTargetURL(WebContents* source, const GURL& url) {
-+  CALL_CEF_DELEGATE(UpdateTargetURL, source, url);
-+
-   std::vector<StatusBubble*> status_bubbles = GetStatusBubbles();
-   for (StatusBubble* status_bubble : status_bubbles) {
-     StatusBubbleViews* status_bubble_views =
-@@ -1550,15 +1634,31 @@ void Browser::ContentsZoomChange(bool zoom_in) {
- }
- 
- bool Browser::TakeFocus(content::WebContents* source, bool reverse) {
-+  CALL_CEF_DELEGATE_RETURN(TakeFocus, source, reverse);
-   return false;
- }
- 
-+void Browser::CanDownload(const GURL& url,
-+                          const std::string& request_method,
-+                          base::OnceCallback<void(bool)> callback) {
-+#if BUILDFLAG(ENABLE_CEF)
-+  if (cef_browser_delegate_) {
-+    cef_browser_delegate_->CanDownload(url, request_method,
-+                                       std::move(callback));
-+    return;
-+  }
-+#endif
-+  std::move(callback).Run(true);
-+}
-+
- bool Browser::DidAddMessageToConsole(
-     content::WebContents* source,
-     blink::mojom::ConsoleMessageLevel log_level,
-     const std::u16string& message,
-     int32_t line_no,
-     const std::u16string& source_id) {
-+  CALL_CEF_DELEGATE_RETURN(DidAddMessageToConsole, source, log_level, message,
-+                           line_no, source_id);
-   static bool is_headless_mode = headless::IsHeadlessMode();
-   if (is_headless_mode) {
-     const bool is_builtin_component = !!source->GetWebUI();
-@@ -1716,12 +1816,23 @@ void Browser::WebContentsCreated(WebContents* source_contents,
-   // to track `new_contents` after it is added to its TabModel this override can
-   // be removed.
-   CreateSessionServiceTabHelper(new_contents);
-+
-+  CALL_CEF_DELEGATE(WebContentsCreated, source_contents, opener_id, frame_name,
-+                    target_url, new_contents);
- }
- 
- void Browser::RendererUnresponsive(
-     WebContents* source,
-     content::RenderWidgetHost* render_widget_host,
-     base::RepeatingClosure hang_monitor_restarter) {
-+#if BUILDFLAG(ENABLE_CEF)
-+  if (cef_browser_delegate_ &&
-+      cef_browser_delegate_->RendererUnresponsiveEx(source, render_widget_host,
-+                                                    hang_monitor_restarter)) {
-+    return;
-+  }
-+#endif
-+
-   // Don't show the page hung dialog when a HTML popup hangs because
-   // the dialog will take the focus and immediately close the popup.
-   RenderWidgetHostView* view = render_widget_host->GetView();
-@@ -1734,6 +1845,13 @@ void Browser::RendererUnresponsive(
- void Browser::RendererResponsive(
-     WebContents* source,
-     content::RenderWidgetHost* render_widget_host) {
-+#if BUILDFLAG(ENABLE_CEF)
-+  if (cef_browser_delegate_ &&
-+      cef_browser_delegate_->RendererResponsiveEx(source, render_widget_host)) {
-+    return;
-+  }
-+#endif
-+
-   RenderWidgetHostView* view = render_widget_host->GetView();
-   if (view && !render_widget_host->GetView()->IsHTMLFormPopup()) {
-     TabDialogs::FromWebContents(source)->HideHungRendererDialog(
-@@ -1743,6 +1861,15 @@ void Browser::RendererResponsive(
- 
- content::JavaScriptDialogManager* Browser::GetJavaScriptDialogManager(
-     WebContents* source) {
-+#if BUILDFLAG(ENABLE_CEF)
-+  if (cef_browser_delegate_) {
-+    auto* cef_js_dialog_manager =
-+        cef_browser_delegate_->GetJavaScriptDialogManager(source);
-+    if (cef_js_dialog_manager) {
-+      return cef_js_dialog_manager;
-+    }
-+  }
-+#endif
-   return javascript_dialogs::TabModalDialogManager::FromWebContents(source);
- }
- 
-@@ -1769,6 +1896,11 @@ void Browser::DraggableRegionsChanged(
-           web_app::AppBrowserController::From(this)) {
-     app_browser_controller->DraggableRegionsChanged(regions, contents);
-   }
-+#if BUILDFLAG(ENABLE_CEF)
-+  else if (cef_delegate()) {
-+    cef_delegate()->DraggableRegionsChanged(regions, contents);
-+  }
-+#endif
- }
- 
- std::vector<blink::mojom::RelatedApplicationPtr>
-@@ -1894,6 +2026,8 @@ void Browser::EnterFullscreenModeForTab(
-       ->fullscreen_controller()
-       ->EnterFullscreenModeForTab(requesting_frame,
-                                   FullscreenTabParams{options.display_id});
-+
-+  CALL_CEF_DELEGATE(EnterFullscreenModeForTab, requesting_frame, options);
- }
- 
- void Browser::ExitFullscreenModeForTab(WebContents* web_contents) {
-@@ -1901,6 +2035,8 @@ void Browser::ExitFullscreenModeForTab(WebContents* web_contents) {
-       ->exclusive_access_manager()
-       ->fullscreen_controller()
-       ->ExitFullscreenModeForTab(web_contents);
-+
-+  CALL_CEF_DELEGATE(ExitFullscreenModeForTab, web_contents);
- }
- 
- bool Browser::IsFullscreenForTabOrPending(const WebContents* web_contents) {
-@@ -2084,6 +2220,18 @@ void Browser::FindReply(WebContents* web_contents,
-   find_tab_helper->HandleFindReply(request_id, number_of_matches,
-                                    selection_rect, active_match_ordinal,
-                                    final_update);
-+  CALL_CEF_DELEGATE(FindReply, web_contents, request_id, number_of_matches,
-+                    selection_rect, active_match_ordinal, final_update);
-+}
-+
-+void Browser::UpdatePreferredSize(WebContents* source,
-+                                  const gfx::Size& pref_size) {
-+  CALL_CEF_DELEGATE(UpdatePreferredSize, source, pref_size);
-+}
-+
-+void Browser::ResizeDueToAutoResize(WebContents* source,
-+                                    const gfx::Size& new_size) {
-+  CALL_CEF_DELEGATE(ResizeDueToAutoResize, source, new_size);
- }
+@@ -864,6 +879,12 @@ void Browser::OnTabReplacedAt(WebContents* old_contents,
+ // Browser, Assorted utility functions (private):
  
- void Browser::RequestPointerLock(WebContents* web_contents,
-@@ -2122,6 +2270,16 @@ void Browser::RequestMediaAccessPermission(
-     content::WebContents* web_contents,
-     const content::MediaStreamRequest& request,
-     content::MediaResponseCallback callback) {
+ void Browser::SetAsDelegate(WebContents* web_contents, bool set_delegate) {
 +#if BUILDFLAG(ENABLE_CEF)
 +  if (cef_browser_delegate_) {
-+    callback = cef_browser_delegate_->RequestMediaAccessPermissionEx(
-+        web_contents, request, std::move(callback));
-+    if (callback.is_null()) {
-+      return;
-+    }
++    cef_browser_delegate_->SetAsDelegate(web_contents, set_delegate);
 +  }
 +#endif
 +
-   const extensions::Extension* extension =
-       GetExtensionForOrigin(profile_, request.security_origin);
-   MediaCaptureDevicesDispatcher::GetInstance()->ProcessMediaAccessRequest(
-@@ -2613,9 +2771,11 @@ void Browser::RemoveScheduledUpdatesFor(WebContents* contents) {
- // Browser, Getters for UI (private):
- 
- std::vector<StatusBubble*> Browser::GetStatusBubbles() {
-+  bool show_by_default = true;
-+
-   // For kiosk and exclusive app mode we want to always hide the status bubble.
-   if (IsRunningInAppMode()) {
--    return {};
-+    show_by_default = false;
-   }
- 
-   // We hide the status bar for web apps windows as this matches native
-@@ -2625,6 +2785,12 @@ std::vector<StatusBubble*> Browser::GetStatusBubbles() {
-       web_app::AppBrowserController::From(this);
-   if (app_browser_controller &&
-       !app_browser_controller->HasMinimalUiButtons()) {
-+    show_by_default = false;
-+  }
-+
-+  bool show = show_by_default;
-+  CALL_CEF_DELEGATE_RESULT(ShowStatusBubble, show, show_by_default);
-+  if (!show) {
-     return {};
-   }
+   content::WebContentsDelegate* delegate =
+       set_delegate ? BrowserWebContentsDelegate::From(this) : nullptr;
  
-@@ -2660,6 +2826,8 @@ void Browser::SetAsDelegate(WebContents* web_contents, bool set_delegate) {
-   } else {
-     web_contents_collection_.StopObserving(web_contents);
-   }
-+
-+  CALL_CEF_DELEGATE(SetAsDelegate, web_contents, set_delegate);
- }
- 
- void Browser::TabDetachedAtImpl(content::WebContents* contents,
 diff --git chrome/browser/ui/browser.h chrome/browser/ui/browser.h
-index 324951ceb77a20ed5eb85a736cd087166218b077..1695077bab63cd098c29cc468860fe5aaebca890 100644
+index a80862e7642648370b91c2c0962fd3538f5fa366..dd7af5f77f9f2051b68ad7899af1ce02bd49e23b 100644
 --- chrome/browser/ui/browser.h
 +++ chrome/browser/ui/browser.h
 @@ -24,6 +24,7 @@
@@ -566,36 +249,28 @@
  #include "chrome/browser/tab_contents/web_contents_collection.h"
  #include "chrome/browser/ui/bookmarks/bookmark_bar.h"
  #include "chrome/browser/ui/bookmarks/bookmark_bar_controller.h"
-@@ -53,6 +54,10 @@
- #include "ui/gfx/geometry/rect.h"
- #include "url/origin.h"
+@@ -79,6 +80,10 @@ namespace content {
+ class NavigationHandle;
+ }  // namespace content
  
 +#if BUILDFLAG(ENABLE_CEF)
 +#include "cef/libcef/browser/chrome/browser_delegate.h"
 +#endif
 +
- #if BUILDFLAG(IS_ANDROID)
- #error This file should only be included on desktop.
- #endif
-@@ -269,6 +274,15 @@ class Browser : public TabStripModelObserver,
-     // Specifies the width for the uncollapsed Vertical Tab Strip.
-     std::optional<int> vertical_tab_strip_uncollapsed_width;
- 
-+#if BUILDFLAG(ENABLE_CEF)
-+    // Opaque CEF-specific configuration. Will be propagated to new Browsers.
-+    scoped_refptr<cef::BrowserDelegate::CreateParams> cef_params;
-+
-+    // Specify the Browser that is opening this popup.
-+    // Currently only used with TYPE_PICTURE_IN_PICTURE and TYPE_DEVTOOLS.
-+    raw_ptr<BrowserWindowInterface, DanglingUntriaged> opener = nullptr;
-+#endif
-+
-    private:
-     friend class Browser;
-     friend class WindowSizerChromeOSTest;
-@@ -327,6 +341,16 @@ class Browser : public TabStripModelObserver,
-     update_ui_immediately_for_testing_ = true;
-   }
+ namespace web_app {
+ class AppBrowserController;
+ }
+@@ -160,6 +165,7 @@ class Browser : public TabStripModelObserver,
+       DidBecomeInactiveCallback callback) override;
+   Type GetType() const override;
+   std::vector<tabs::TabInterface*> GetAllTabInterfaces() override;
++  bool IsNormalBrowser() const override;
+   bool IsTabModalPopup() const override;
+   void SetIsTabModalPopup(
+       bool is_tab_modal_popup,
+@@ -170,6 +176,22 @@ class Browser : public TabStripModelObserver,
+   DesktopBrowserWindowCapabilities* capabilities() override;
+   const DesktopBrowserWindowCapabilities* capabilities() const override;
  
 +  // Return true if CEF will expose the toolbar to the client. This value is
 +  // used to selectively enable toolbar behaviors such as command processing
@@ -607,69 +282,40 @@
 +  // Used when the BrowserWindow will outlive this Browser.
 +  void ReleaseBrowserWindow() { window_.release(); }
 +
-   // Accessors ////////////////////////////////////////////////////////////////
- 
-   Type type() const { return type_; }
-@@ -349,6 +373,12 @@ class Browser : public TabStripModelObserver,
-     return features_.get();
-   }
- 
 +#if BUILDFLAG(ENABLE_CEF)
-+  cef::BrowserDelegate* cef_delegate() const {
++  cef::BrowserDelegate* cef_delegate() const override {
 +    return cef_browser_delegate_.get();
 +  }
 +#endif
 +
-   base::WeakPtr<Browser> AsWeakPtr();
-   base::WeakPtr<const Browser> AsWeakPtr() const;
- 
-@@ -527,6 +557,7 @@ class Browser : public TabStripModelObserver,
-       DidBecomeInactiveCallback callback) override;
-   BrowserActions* GetActions() override;
-   Type GetType() const override;
-+  bool IsNormalBrowser() const override;
-   std::vector<tabs::TabInterface*> GetAllTabInterfaces() override;
-   Browser* GetBrowserForMigrationOnly() override;
-   const Browser* GetBrowserForMigrationOnly() const override;
-@@ -615,6 +646,9 @@ class Browser : public TabStripModelObserver,
-                           const ui::Event& event) override;
-   void ContentsZoomChange(bool zoom_in) override;
-   bool TakeFocus(content::WebContents* source, bool reverse) override;
-+  void CanDownload(const GURL& url,
-+                   const std::string& request_method,
-+                   base::OnceCallback<void(bool)> callback) override;
-   bool DidAddMessageToConsole(content::WebContents* source,
-                               blink::mojom::ConsoleMessageLevel log_level,
-                               const std::u16string& message,
-@@ -705,6 +739,10 @@ class Browser : public TabStripModelObserver,
-                  const gfx::Rect& selection_rect,
-                  int active_match_ordinal,
-                  bool final_update) override;
-+  void UpdatePreferredSize(content::WebContents* source,
-+                           const gfx::Size& pref_size) override;
-+  void ResizeDueToAutoResize(content::WebContents* source,
-+                             const gfx::Size& new_size) override;
-   void RequestPointerLock(content::WebContents* web_contents,
-                           bool user_gesture,
-                           bool last_unlocked_by_target) override;
-@@ -889,6 +927,12 @@ class Browser : public TabStripModelObserver,
-   // The active state of this browser.
-   bool is_active_ = false;
+  private:
+   friend BrowserWindowInterface* CreateBrowserWindow(
+       BrowserWindowCreateParams create_params);
+@@ -274,6 +296,10 @@ class Browser : public TabStripModelObserver,
+   // This Browser's window.
+   std::unique_ptr<BrowserWindow, BrowserWindowDeleter> window_;
  
 +#if BUILDFLAG(ENABLE_CEF)
 +  std::unique_ptr<cef::BrowserDelegate> cef_browser_delegate_;
 +#endif
 +
-+  bool toolbar_overridden_ = false;
-+
    std::unique_ptr<TabStripModelDelegate> const tab_strip_model_delegate_;
    std::unique_ptr<TabStripModel> const tab_strip_model_;
  
+@@ -286,6 +312,8 @@ class Browser : public TabStripModelObserver,
+ 
+   WebContentsCollection web_contents_collection_{this};
+ 
++  bool toolbar_overridden_ = false;
++
+   // If true, the browser window was created as a tab modal pop-up.
+   bool is_tab_modal_popup_ = false;
+ 
 diff --git chrome/browser/ui/browser_manager_service.cc chrome/browser/ui/browser_manager_service.cc
-index 3a9c66792158d0a7baba089287029a5eae346544..edda939d9799fef16b1c828829e7f23ee727e9f1 100644
+index ad710a51122632d8efd5135cb1ffbed168f807bb..91cd94f67561df9e21ecbc97a4b8822e069270cc 100644
 --- chrome/browser/ui/browser_manager_service.cc
 +++ chrome/browser/ui/browser_manager_service.cc
-@@ -128,20 +128,28 @@ void BrowserManagerService::DeleteBrowser(Browser* removed_browser) {
+@@ -141,20 +141,28 @@ void BrowserManagerService::DeleteBrowser(
    //
    // TODO(crbug.com/40159237): Use ScopedProfileKeepAlive for Incognito too,
    // instead of separate logic for Incognito and regular profiles.
@@ -704,37 +350,31 @@
  
    // If we're exiting, send out the APP_TERMINATING notification to allow other
 diff --git chrome/browser/ui/browser_tabstrip.cc chrome/browser/ui/browser_tabstrip.cc
-index 0b4f8241e9d4e114d90f3c1747da5cea4ad8c186..e1f9c1ef1e74307cfc4899cccdf27b6de61b09bb 100644
+index bccf4c6a0c037ad620eaf30340c0b6688fa81afd..23c97d79c4be50b7372f7c5e367c47992c420950 100644
 --- chrome/browser/ui/browser_tabstrip.cc
 +++ chrome/browser/ui/browser_tabstrip.cc
-@@ -35,11 +35,16 @@ content::WebContents* AddAndReturnTabAt(
-   // Time new tab page creation time.  We keep track of the timing data in
-   // WebContents, but we want to include the time it takes to create the
-   // WebContents object too.
+@@ -48,7 +48,12 @@ content::WebContents* AddAndReturnTabAt(
+     bool pinned,
+     std::optional<NavigateParams::WindowAction> window_action) {
+   const GURL resolved_url = url.is_empty() ? GetNewTabURL(browser) : url;
+-  NavigateParams params(browser, resolved_url, ui::PAGE_TRANSITION_TYPED);
 +  // For CEF use a PageTransition that matches
 +  // CefFrameHostImpl::kPageTransitionExplicit.
-   base::TimeTicks new_tab_start_time = base::TimeTicks::Now();
-   const GURL resolved_url =
-       url.is_empty() ? browser->GetBrowserForMigrationOnly()->GetNewTabURL()
-                      : url;
--  NavigateParams params(browser, resolved_url, ui::PAGE_TRANSITION_TYPED);
 +  NavigateParams params(
 +      browser, resolved_url,
 +      static_cast<ui::PageTransition>(ui::PAGE_TRANSITION_TYPED |
 +                                      ui::PAGE_TRANSITION_FROM_ADDRESS_BAR));
    params.disposition = foreground ? WindowOpenDisposition::NEW_FOREGROUND_TAB
                                    : WindowOpenDisposition::NEW_BACKGROUND_TAB;
-   params.tabstrip_index = idx;
-@@ -98,6 +103,19 @@ content::WebContents* AddWebContents(
+   if (window_action) {
+@@ -107,6 +112,17 @@ content::WebContents* AddWebContents(
    // Can't create a new contents for the current tab - invalid case.
    DCHECK(disposition != WindowOpenDisposition::CURRENT_TAB);
  
 +#if BUILDFLAG(ENABLE_CEF)
 +  if (browser && new_contents) {
-+    if (auto* cef_delegate =
-+            browser->GetBrowserForMigrationOnly()->cef_delegate()) {
-+      new_contents =
-+          cef_delegate->AddWebContents(std::move(new_contents));
++    if (auto* cef_delegate = browser->cef_delegate()) {
++      new_contents = cef_delegate->AddWebContents(std::move(new_contents));
 +      if (!new_contents) {
 +        return nullptr;
 +      }
@@ -745,36 +385,115 @@
    NavigateParams params(browser, std::move(new_contents));
    params.source_contents = source_contents;
    params.url = target_url;
-@@ -121,8 +139,6 @@ void CloseWebContents(Browser* browser,
+@@ -130,8 +146,6 @@ void CloseWebContents(BrowserWindowInterface* browser,
                        bool add_to_history) {
-   int index = browser->tab_strip_model()->GetIndexOfWebContents(contents);
+   int index = browser->GetTabStripModel()->GetIndexOfWebContents(contents);
    if (index == TabStripModel::kNoTab) {
 -    DUMP_WILL_BE_NOTREACHED()
 -        << "CloseWebContents called for tab not in our strip";
      return;
    }
  
+diff --git chrome/browser/ui/browser_ui_controller/browser_ui_controller.cc chrome/browser/ui/browser_ui_controller/browser_ui_controller.cc
+index 60a3a7ebaa07d600ec4143736b5ceaaca01f74a9..8bd9a7eede20d81dab9bcf3fcef40c2e09a1902e 100644
+--- chrome/browser/ui/browser_ui_controller/browser_ui_controller.cc
++++ chrome/browser/ui/browser_ui_controller/browser_ui_controller.cc
+@@ -11,8 +11,10 @@
+ #include "base/check_op.h"
+ #include "base/task/single_thread_task_runner.h"
+ #include "base/trace_event/trace_event.h"
++#include "cef/libcef/features/features.h"
+ #include "chrome/browser/app_mode/app_mode_utils.h"
+ #include "chrome/browser/ui/bookmarks/bookmark_bar_controller.h"
++#include "chrome/browser/ui/browser.h"
+ #include "chrome/browser/ui/browser_command_controller.h"
+ #include "chrome/browser/ui/browser_window.h"
+ #include "chrome/browser/ui/browser_window/public/browser_window_interface.h"
+@@ -141,7 +143,11 @@ void BrowserUiController::ScheduleUIUpdate(content::WebContents* source,
+     // Update the loading state synchronously. This is so the throbber will
+     // immediately start/stop, which gives a more snappy feel. We want to do
+     // this for any tab so they start & stop quickly.
+-    NotifyTabUIChanged(tab, TabChangeType::kLoadingOnly);
++    // Guard against notifications during tab insertion when the tab strip model
++    // state is not yet fully consistent.
++    if (tab_strip_model_->GetActiveWebContents()) {
++      NotifyTabUIChanged(tab, TabChangeType::kLoadingOnly);
++    }
+     // The status bubble needs to be updated during INVALIDATE_TYPE_LOAD too,
+     // but we do that asynchronously by not stripping INVALIDATE_TYPE_LOAD from
+     // changed_flags.
+@@ -257,9 +263,11 @@ void BrowserUiController::NotifyTabUIChanged(tabs::TabInterface* tab,
+ }
+ 
+ std::vector<StatusBubble*> BrowserUiController::GetStatusBubbles() {
++  bool show_by_default = true;
++
+   // For kiosk and exclusive app mode we want to always hide the status bubble.
+   if (IsRunningInAppMode()) {
+-    return {};
++    show_by_default = false;
+   }
+ 
+   // We hide the status bar for web apps windows as this matches native
+@@ -269,6 +277,16 @@ std::vector<StatusBubble*> BrowserUiController::GetStatusBubbles() {
+       web_app::AppBrowserController::From(&*browser_);
+   if (app_browser_controller &&
+       !app_browser_controller->HasMinimalUiButtons()) {
++    show_by_default = false;
++  }
++
++  bool show = show_by_default;
++#if BUILDFLAG(ENABLE_CEF)
++  if (browser_->cef_delegate()) {
++    show = browser_->cef_delegate()->ShowStatusBubble(show_by_default);
++  }
++#endif
++  if (!show) {
+     return {};
+   }
+ 
 diff --git chrome/browser/ui/browser_window/internal/browser_window_features.cc chrome/browser/ui/browser_window/internal/browser_window_features.cc
-index 8efb317f70aded0e916ba991e9958238307d5f7f..b855aebe41e2a4d2e6a91c95455a80129ded33eb 100644
+index 89cd12a061a3136bf035f509403d453d018b0d78..c98db8aeaa05f5e6e60c0c15381db30a25bae4d6 100644
 --- chrome/browser/ui/browser_window/internal/browser_window_features.cc
 +++ chrome/browser/ui/browser_window/internal/browser_window_features.cc
-@@ -237,6 +237,15 @@
+@@ -12,6 +12,7 @@
+ #include "base/memory/ptr_util.h"
+ #include "base/metrics/histogram_functions.h"
+ #include "base/no_destructor.h"
++#include "cef/libcef/features/features.h"
+ #include "chrome/browser/actor/ui/actor_border_view_controller.h"
+ #include "chrome/browser/actor/ui/actor_ui_window_controller.h"
+ #include "chrome/browser/autocomplete/aim_eligibility_service_factory.h"
+@@ -245,6 +246,17 @@
  BrowserWindowFeatures::BrowserWindowFeatures() = default;
  BrowserWindowFeatures::~BrowserWindowFeatures() = default;
  
 +// static
-+bool BrowserWindowFeatures::IsNormalBrowser(const Browser* browser) {
++bool BrowserWindowFeatures::IsNormalBrowser(
++    const BrowserWindowInterface* browser) {
 +  // CEF normal browsers have TYPE_POPUP.
-+  if (browser->is_type_popup() && browser->cef_delegate()) {
++  if (browser->GetType() == BrowserWindowInterface::Type::TYPE_POPUP &&
++      browser->cef_delegate()) {
 +    return true;
 +  }
-+  return browser->is_type_normal();
++  return browser->GetType() == BrowserWindowInterface::Type::TYPE_NORMAL;
 +}
 +
  void BrowserWindowFeatures::Init(BrowserWindowInterface* browser) {
    // This is used only for the controllers which will be created on demand
    // later.
-@@ -636,6 +645,9 @@ void BrowserWindowFeatures::InitPostWindowConstruction(Browser* browser) {
+@@ -317,8 +329,8 @@ void BrowserWindowFeatures::Init(BrowserWindowInterface* browser) {
+           *browser, *browser, *browser->GetTabStripModel());
+   window_feature_controller_ =
+       GetUserDataFactory().CreateInstance<WindowFeatureController>(
+-          *browser, fullscreen_controller_.get(), app_browser_controller_.get(),
+-          browser->GetType(),
++          *browser, browser, fullscreen_controller_.get(),
++          app_browser_controller_.get(), browser->GetType(),
+           BrowserInitState::From(browser)
+               ->browser_window_create_params()
+               .is_trusted_source,
+@@ -682,6 +694,9 @@ void BrowserWindowFeatures::InitPostWindowConstruction(Browser* browser) {
    // Foundational state used throughout this function. Computed/assigned early
    // so the alphabetical sections below can rely on them.
    Profile* const profile = browser_->GetProfile();
@@ -784,7 +503,7 @@
    BrowserView* const browser_view =
        BrowserView::GetBrowserViewForBrowser(browser);
    WebUIBrowserWindow* const webui_browser_window =
-@@ -700,18 +712,6 @@ void BrowserWindowFeatures::InitPostWindowConstruction(Browser* browser) {
+@@ -752,18 +767,6 @@ void BrowserWindowFeatures::InitPostWindowConstruction(Browser* browser) {
          std::make_unique<ContentsBorderController>(browser_view);
    }
  
@@ -802,26 +521,39 @@
 -
  #if BUILDFLAG(ENABLE_EXTENSIONS) && (BUILDFLAG(IS_WIN) || BUILDFLAG(IS_MAC))
    if (browser_view) {
-     if (base::FeatureList::IsEnabled(
-@@ -849,7 +849,7 @@ void BrowserWindowFeatures::InitPostWindowConstruction(Browser* browser) {
+     if (!browser_->GetProfile()->IsOffTheRecord()) {
+@@ -813,7 +816,12 @@ void BrowserWindowFeatures::InitPostWindowConstruction(Browser* browser) {
+ 
+   // Must be after exclusive_access_manager_,
+   // desktop_browser_window_capabilities_, and browser_ui_controller_.
++#if BUILDFLAG(ENABLE_CEF)
++  browser_web_contents_delegate_ =
++      cef::BrowserDelegate::CreateWebContentsDelegate(
++#else
+   browser_web_contents_delegate_ = std::make_unique<BrowserWebContentsDelegate>(
++#endif
+       browser, *exclusive_access_manager_, *browser_command_controller_,
+       *unload_controller_, app_browser_controller_.get(),
+       *BrowserWindow::FromBrowser(browser),
+@@ -926,7 +934,7 @@ void BrowserWindowFeatures::InitPostWindowConstruction(Browser* browser) {
        browser, browser->GetTabStripModel(), browser->GetSessionID(),
        browser->GetType());
  
--  if (browser->is_type_normal() || browser->is_type_app()) {
-+  if (supports_toolbar || browser->is_type_app()) {
-     toast_service_ = std::make_unique<ToastService>(browser);
-   }
- 
-@@ -887,7 +887,7 @@ void BrowserWindowFeatures::InitPostWindowConstruction(Browser* browser) {
+-  if (browser->GetType() == BrowserWindowInterface::Type::TYPE_NORMAL ||
++  if (supports_toolbar ||
+       browser->GetType() == BrowserWindowInterface::Type::TYPE_APP) {
+     toast_service_ =
+         GetUserDataFactory().CreateInstance<ToastService>(*browser, browser);
+@@ -970,7 +978,7 @@ void BrowserWindowFeatures::InitPostWindowConstruction(Browser* browser) {
    // omnibox and a tab strip). By default most new features should be
    // instantiated in this block (please keep this list ordered without taking
    // into consideration buildflags, repeating buildflags is ok):
--  if (browser->is_type_normal()) {
+-  if (browser->GetType() == BrowserWindowInterface::Type::TYPE_NORMAL) {
 +  if (supports_toolbar) {
      if (browser_view) {
        if (base::FeatureList::IsEnabled(features::kGlicActorUi)) {
          std::vector<std::pair<views::WebView*, ActorOverlayWebView*>>
-@@ -1041,6 +1041,38 @@ void BrowserWindowFeatures::InitPostBrowserViewConstruction(
+@@ -1135,6 +1143,38 @@ void BrowserWindowFeatures::InitPostBrowserViewConstruction(
        browser_view);
  }
  
@@ -861,29 +593,29 @@
    // Tear down in reverse order of construction: embedder first, then
    // InitPostWindowConstruction, then Init. Within each section, members are
 diff --git chrome/browser/ui/browser_window/public/browser_window_features.h chrome/browser/ui/browser_window/public/browser_window_features.h
-index 97e293aa77130a343cfdf46ad567a1a3da54718e..d2845af6e8f7fc249b6769cebfdfb6fdf36bfaf0 100644
+index 562496763d418d8e0d897a269ff3e3c7285030dc..0dff344929fd9acf68578338175cb75e682cdaa3 100644
 --- chrome/browser/ui/browser_window/public/browser_window_features.h
 +++ chrome/browser/ui/browser_window/public/browser_window_features.h
-@@ -137,6 +137,9 @@ class CookieControlsController;
+@@ -173,6 +173,9 @@ namespace content_settings {
+ class CookieControlsController;
  }  // namespace content_settings
  
- 
 +namespace content {
 +class WebContents;
 +}
  namespace contextual_tasks {
  class ContextualTasksBrowserController;
  }  // namespace contextual_tasks
-@@ -262,6 +265,8 @@ class BrowserWindowFeatures {
+@@ -298,6 +301,8 @@ class BrowserWindowFeatures {
    BrowserWindowFeatures();
    ~BrowserWindowFeatures();
  
-+  static bool IsNormalBrowser(const Browser* browser);
++  static bool IsNormalBrowser(const BrowserWindowInterface* browser);
 +
    BrowserWindowFeatures(const BrowserWindowFeatures&) = delete;
    BrowserWindowFeatures& operator=(const BrowserWindowFeatures&) = delete;
  
-@@ -278,6 +283,16 @@ class BrowserWindowFeatures {
+@@ -314,6 +319,16 @@ class BrowserWindowFeatures {
    // hierarchy in BrowserView.
    void InitPostBrowserViewConstruction(BrowserView* browser_view);
  
@@ -901,77 +633,112 @@
    void TearDownPreBrowserWindowDestruction();
  
 diff --git chrome/browser/ui/browser_window/public/browser_window_interface.h chrome/browser/ui/browser_window/public/browser_window_interface.h
-index 5374510d7c3f360748ec598f676ded178981dba4..bf8c6ec25aa48bde0032d6b2aa5695d650a6f7ae 100644
+index 5d1a05d3c55e84d7a8fc6bb23fda2cb6600fad29..25e60d663a4f59a45fcfe33564fa76686f5e3c76 100644
 --- chrome/browser/ui/browser_window/public/browser_window_interface.h
 +++ chrome/browser/ui/browser_window/public/browser_window_interface.h
-@@ -324,6 +324,10 @@ class BrowserWindowInterface : public content::PageNavigator {
-   // browser window (e.g. most of the 3-dot menu actions).
-   virtual BrowserActions* GetActions() = 0;
+@@ -9,6 +9,7 @@
+ 
+ #include "base/memory/raw_ptr.h"
+ #include "build/build_config.h"
++#include "cef/libcef/features/features.h"
+ #include "content/public/browser/page_navigator.h"
+ 
+ #if !BUILDFLAG(IS_ANDROID)
+@@ -57,6 +58,12 @@ class UnownedUserDataHost;
+ class Profile;
+ class SessionID;
+ 
++#if BUILDFLAG(ENABLE_CEF)
++namespace cef {
++class BrowserDelegate;
++}
++#endif
++
+ namespace internal {
+ class ScopedBrowserShower;
+ }
+@@ -318,6 +325,14 @@ class BrowserWindowInterface : public content::PageNavigator {
+   virtual base::CallbackListSubscription RegisterDidBecomeInactive(
+       DidBecomeInactiveCallback callback) = 0;
  
 +  // Returns true if this is a normal browser as determined by
 +  // BrowserWindowFeatures::IsNormalBrowser.
 +  virtual bool IsNormalBrowser() const = 0;
 +
++#if BUILDFLAG(ENABLE_CEF)
++  virtual cef::BrowserDelegate* cef_delegate() const { return nullptr; }
++#endif
++
    // This is used by features that need to operate on most or all tabs in the
    // browser window. Do not use this method to find a specific tab.
    virtual std::vector<tabs::TabInterface*> GetAllTabInterfaces() = 0;
-diff --git chrome/browser/ui/navigator/browser_navigator.cc chrome/browser/ui/navigator/browser_navigator.cc
-index a3a2000fe57f7095a85dbbcbbff473d41fdb83b5..f8f4400defe93ddbbb17d05fcb3c11bda755e111 100644
---- chrome/browser/ui/navigator/browser_navigator.cc
-+++ chrome/browser/ui/navigator/browser_navigator.cc
-@@ -31,6 +31,7 @@
- #include "chrome/browser/ui/browser.h"
- #include "chrome/browser/ui/browser_init_state.h"
- #include "chrome/browser/ui/browser_window.h"
-+#include "chrome/browser/ui/browser_window/public/browser_window_features.h"
+diff --git chrome/browser/ui/browser_window/public/create_browser_window.h chrome/browser/ui/browser_window/public/create_browser_window.h
+index c6e44f9a4bc2653d58eff131d9692b98b88d43f1..01cc9425dc8eb10e27384e0e1f11e894926be423 100644
+--- chrome/browser/ui/browser_window/public/create_browser_window.h
++++ chrome/browser/ui/browser_window/public/create_browser_window.h
+@@ -13,10 +13,15 @@
+ #include "base/memory/raw_ptr.h"
+ #include "base/memory/raw_ref.h"
+ #include "build/build_config.h"
++#include "cef/libcef/features/features.h"
  #include "chrome/browser/ui/browser_window/public/browser_window_interface.h"
- #include "chrome/browser/ui/browser_window/public/profile_browser_collection.h"
- #include "chrome/browser/ui/incognito_allowed_url.h"
-@@ -312,6 +313,12 @@ std::tuple<BrowserWindowInterface*, int> GetBrowserAndTabForDisposition(
- 
-       browser_params.pip_options = pip_options;
+ #include "ui/base/mojom/window_show_state.mojom.h"
+ #include "ui/gfx/geometry/rect.h"
  
-+      browser_params.can_resize = pip_options->resizable;
-+
 +#if BUILDFLAG(ENABLE_CEF)
-+      browser_params.opener = params.browser;
++#include "cef/libcef/browser/chrome/browser_delegate.h"
 +#endif
 +
-       const ui::BaseWindow* const browser_window = params.browser->GetWindow();
-       const gfx::NativeWindow native_window =
-           browser_window ? browser_window->GetNativeWindow()
-@@ -501,7 +508,13 @@ std::unique_ptr<content::WebContents> CreateTargetContents(
-   }
- #endif
- 
--  return WebContents::Create(create_params);
-+  std::unique_ptr<WebContents> target_contents =
-+      WebContents::Create(create_params);
-+
-+  params.browser->GetFeatures().OnWebContentsCreated(params.browser,
-+                                                     target_contents.get());
-+
-+  return target_contents;
+ #if !BUILDFLAG(IS_ANDROID)
+ #include "components/tab_groups/tab_group_id.h"
+ #include "third_party/blink/public/mojom/picture_in_picture_window_options/picture_in_picture_window_options.mojom.h"
+@@ -171,6 +176,15 @@ struct BrowserWindowCreateParams {
+   // Specifies the width for the uncollapsed Vertical Tab Strip.
+   std::optional<int> vertical_tab_strip_uncollapsed_width;
+ 
++#if BUILDFLAG(ENABLE_CEF)
++  // Opaque CEF-specific configuration. Will be propagated to new Browsers.
++  scoped_refptr<cef::BrowserDelegate::CreateParams> cef_params;
++
++  // Specify the Browser that is opening this popup.
++  // Currently only used with TYPE_PICTURE_IN_PICTURE and TYPE_DEVTOOLS.
++  raw_ptr<BrowserWindowInterface, DanglingUntriaged> opener = nullptr;
++#endif
++
+   // Specifies the focused tab group ID, if the window should be created in a
+   // focused state.
+   std::optional<tab_groups::TabGroupId> focused_tab_group_id;
+diff --git chrome/browser/ui/tabs/tab_strip_api/tab_strip_model_impl/tab_strip_model_adapter_impl.cc chrome/browser/ui/tabs/tab_strip_api/tab_strip_model_impl/tab_strip_model_adapter_impl.cc
+index 4f84323baa4e70a6a1545783209aaee18a6ebbc2..2c3ae956de76665b9f9bc5dd3a8f68a66b7b7b3b 100644
+--- chrome/browser/ui/tabs/tab_strip_api/tab_strip_model_impl/tab_strip_model_adapter_impl.cc
++++ chrome/browser/ui/tabs/tab_strip_api/tab_strip_model_impl/tab_strip_model_adapter_impl.cc
+@@ -13,6 +13,8 @@
+ #include "components/tab_groups/tab_group_visual_data.h"
+ #include "components/tabs/public/split_tab_collection.h"
+ #include "components/tabs/public/tab_collection.h"
++#include "ui/color/color_provider_manager.h"
++#include "ui/native_theme/native_theme.h"
+ #include "components/tabs/public/tab_group.h"
+ #include "components/tabs/public/tab_group_tab_collection.h"
+ #include "components/tabs/public/tab_interface.h"
+@@ -68,7 +70,11 @@ types::TabStates TabStripModelAdapterImpl::GetTabStates(
+ const ui::ColorProvider& TabStripModelAdapterImpl::GetColorProvider() const {
+   content::WebContents* active_contents =
+       tab_strip_model_->GetActiveWebContents();
+-  CHECK(active_contents);
++  if (!active_contents) {
++    return *ui::ColorProviderManager::Get().GetColorProviderFor(
++        ui::NativeTheme::GetInstanceForNativeUi()->GetColorProviderKey(
++            /*custom_theme=*/nullptr));
++  }
+   return active_contents->GetColorProvider();
  }
  
- }  // namespace
-@@ -921,6 +934,11 @@ base::WeakPtr<content::NavigationHandle> NavigateImpl(
-     // navigation happens in a different tab to the link click.
-     apps::SetLinkCapturingSourceDisposition(tab_to_insert->GetContents(),
-                                             params->disposition);
-+
-+    // With CEF the BrowserView is created before the TabModel, so we need a
-+    // separate initialization of objects that depend on TabModel.
-+    params->browser->GetFeatures().InitPostTabModelConstruction(
-+        params->browser);
-   }
- 
-   if (params->source_contents == contents_to_navigate_or_insert) {
 diff --git chrome/browser/ui/views/frame/horizontal_tab_strip_region_view.cc chrome/browser/ui/views/frame/horizontal_tab_strip_region_view.cc
-index f994a992e8eaa8124d8633239ec3f13cd43c4b62..988b5b9b15781a42ac0c4c9e335a96dc8613eeb6 100644
+index 76e408d4225449b8b9cbde50579d3396f5a03844..c346a4b483ca95e4938ec531d9ebb5161cbe1aba 100644
 --- chrome/browser/ui/views/frame/horizontal_tab_strip_region_view.cc
 +++ chrome/browser/ui/views/frame/horizontal_tab_strip_region_view.cc
-@@ -205,8 +205,7 @@ HorizontalTabStripRegionViewOld::HorizontalTabStripRegionViewOld(
+@@ -201,8 +201,7 @@ HorizontalTabStripRegionViewOld::HorizontalTabStripRegionViewOld(
  
    // Add and configure the TabStripComboButton.
    std::unique_ptr<TabStripActionContainer> tab_strip_action_container;
@@ -981,11 +748,77 @@
      // The Glic button visibility is dynamic and depends on profile state
      // (e.g., sign-in status, enterprise policies, recoverable errors).
      // We instantiate the action container if the profile is eligible (even if
+diff --git chrome/browser/ui/window_feature_controller/window_feature_controller.cc chrome/browser/ui/window_feature_controller/window_feature_controller.cc
+index ec7bd3ea6cd00a5093b9bac2cfca9fe1a0bc4bfb..ec531f37cd4a3fa404f85dd98ca56451339226b7 100644
+--- chrome/browser/ui/window_feature_controller/window_feature_controller.cc
++++ chrome/browser/ui/window_feature_controller/window_feature_controller.cc
+@@ -7,18 +7,25 @@
+ #include "base/check.h"
+ #include "base/check_deref.h"
+ #include "build/build_config.h"
++#include "cef/libcef/features/features.h"
+ #include "chrome/browser/ui/fullscreen/browser_window_fullscreen_controller.h"
+ #include "chrome/browser/ui/web_applications/app_browser_controller.h"
+ 
++#if BUILDFLAG(ENABLE_CEF)
++#include "cef/libcef/browser/chrome/browser_delegate.h"
++#endif
++
+ DEFINE_USER_DATA(WindowFeatureController);
+ 
+ WindowFeatureController::WindowFeatureController(
++    BrowserWindowInterface* browser,
+     BrowserWindowFullscreenController* fullscreen_controller,
+     web_app::AppBrowserController* app_controller,
+     BrowserWindowInterface::Type browser_type,
+     bool is_trusted_source,
+     ui::UnownedUserDataHost& host)
+-    : fullscreen_controller_(CHECK_DEREF(fullscreen_controller)),
++    : browser_(browser),
++      fullscreen_controller_(CHECK_DEREF(fullscreen_controller)),
+       app_controller_(app_controller),
+       browser_type_(browser_type),
+       is_trusted_source_(is_trusted_source),
+@@ -42,6 +49,14 @@ const WindowFeatureController* WindowFeatureController::From(
+ 
+ bool WindowFeatureController::SupportsWindowFeature(
+     WindowFeature feature) const {
++#if BUILDFLAG(ENABLE_CEF)
++  if (browser_->cef_delegate()) {
++    if (auto value = browser_->cef_delegate()->SupportsWindowFeature(
++            static_cast<int>(feature))) {
++      return *value;
++    }
++  }
++#endif
+   bool supports =
+       SupportsWindowFeatureImpl(feature, /*check_can_support=*/false);
+   // Supported features imply CanSupportWindowFeature.
+diff --git chrome/browser/ui/window_feature_controller/window_feature_controller.h chrome/browser/ui/window_feature_controller/window_feature_controller.h
+index 7c203d12c129f2a7b949c5ccb5144d04b175d58b..efcc0206789d58cda43579cd6177039834dec323 100644
+--- chrome/browser/ui/window_feature_controller/window_feature_controller.h
++++ chrome/browser/ui/window_feature_controller/window_feature_controller.h
+@@ -34,6 +34,7 @@ class WindowFeatureController {
+   };
+ 
+   WindowFeatureController(
++      BrowserWindowInterface* browser,
+       BrowserWindowFullscreenController* fullscreen_controller,
+       web_app::AppBrowserController* app_controller,
+       BrowserWindowInterface::Type browser_type,
+@@ -85,6 +86,7 @@ class WindowFeatureController {
+                                  bool check_can_support) const;
+   bool IsFullscreen() const;
+ 
++  const raw_ptr<BrowserWindowInterface> browser_;
+   const raw_ref<BrowserWindowFullscreenController> fullscreen_controller_;
+ 
+   // Can be null.
 diff --git chrome/browser/ui/window_metadata/window_metadata_controller.cc chrome/browser/ui/window_metadata/window_metadata_controller.cc
-index efdeb4080a66c06cb020529f63d246732f51249b..4ffaab731491243a6cc9d93bc2a4e95ff9f3b9a8 100644
+index 7b132613b32a3443a3ced92470447f2de0414fdd..360b01ea84c742a205ec35a566c6a6cdf46357e6 100644
 --- chrome/browser/ui/window_metadata/window_metadata_controller.cc
 +++ chrome/browser/ui/window_metadata/window_metadata_controller.cc
-@@ -63,8 +63,12 @@ WindowMetadataController::WindowMetadataController(
+@@ -63,8 +63,13 @@ WindowMetadataController::WindowMetadataController(
  WindowMetadataController::~WindowMetadataController() = default;
  
  gfx::Image WindowMetadataController::GetCurrentPageIcon() const {
@@ -993,7 +826,8 @@
 +  // WebContents instead of the picture-in-picture WebContents itself.
    content::WebContents* web_contents =
 -      browser_->tab_strip_model()->GetActiveWebContents();
-+      browser_->is_type_picture_in_picture()
++      (browser_->GetType() ==
++       BrowserWindowInterface::Type::TYPE_PICTURE_IN_PICTURE)
 +          ? PictureInPictureWindowManager::GetInstance()->GetWebContents()
 +          : browser_->tab_strip_model()->GetActiveWebContents();
    // |web_contents| can be NULL since GetCurrentPageIcon() is called by the
diff -urN cef-7977-orig/patch/patches/chrome_browser_frame_mac.patch cef-7977/patch/patches/chrome_browser_frame_mac.patch
--- cef-7977-orig/patch/patches/chrome_browser_frame_mac.patch	2026-08-20 16:03:41.000000000 +0200
+++ cef-7977/patch/patches/chrome_browser_frame_mac.patch	2026-09-16 19:42:56.675940057 +0200
@@ -1,5 +1,5 @@
 diff --git chrome/browser/ui/views/frame/browser_native_widget_mac.h chrome/browser/ui/views/frame/browser_native_widget_mac.h
-index 79bfe09293c526168b815808db4f8cae9b5418ed..8aa86ae78e82370cb59c0cb96403780a8f24b120 100644
+index 927117fb0565681bd25261211e48188cd011ebc9..07b253059d7a43f6f6c619a649b6b734de9d9de8 100644
 --- chrome/browser/ui/views/frame/browser_native_widget_mac.h
 +++ chrome/browser/ui/views/frame/browser_native_widget_mac.h
 @@ -18,6 +18,7 @@
@@ -10,50 +10,49 @@
  class BrowserWidget;
  class BrowserView;
  @class BrowserWindowTouchBarController;
-@@ -67,6 +68,21 @@ class BrowserNativeWidgetMac : public views::NativeWidgetMac,
+@@ -70,6 +71,19 @@ class BrowserNativeWidgetMac : public views::NativeWidgetMac,
    bool ShouldUseInitialVisibleOnAllWorkspaces() const override;
    void AnnounceTextInInProcessWindow(const std::u16string& text) override;
  
 +  static void ValidateUserInterfaceItem(
-+      Browser* browser,
++      BrowserWindowInterface* browser,
 +      int32_t command,
 +      remote_cocoa::mojom::ValidateUserInterfaceItemResult* result);
-+  static bool WillExecuteCommand(
-+      Browser* browser,
-+      int32_t command,
-+      WindowOpenDisposition window_open_disposition,
-+      bool is_before_first_responder);
-+  static bool ExecuteCommand(
-+      Browser* browser,
-+      int32_t command,
-+      WindowOpenDisposition window_open_disposition,
-+      bool is_before_first_responder);
++  static bool WillExecuteCommand(BrowserWindowInterface* browser,
++                                 int32_t command,
++                                 WindowOpenDisposition window_open_disposition,
++                                 bool is_before_first_responder);
++  static bool ExecuteCommand(BrowserWindowInterface* browser,
++                             int32_t command,
++                             WindowOpenDisposition window_open_disposition,
++                             bool is_before_first_responder);
 +
   protected:
    ~BrowserNativeWidgetMac() override;
  
 diff --git chrome/browser/ui/views/frame/browser_native_widget_mac.mm chrome/browser/ui/views/frame/browser_native_widget_mac.mm
-index 93eeda2daf44d3da4591852d1a82a0aeb9a969b4..e08731042847f3790fe2a36b9b5396d35e73c878 100644
+index fb1e87f83493475668792b1f4b59b4c4e777162f..0ab9ba2e66efd6ec30e3577d1cdfb005ac25dd50 100644
 --- chrome/browser/ui/views/frame/browser_native_widget_mac.mm
 +++ chrome/browser/ui/views/frame/browser_native_widget_mac.mm
-@@ -270,7 +270,15 @@ void BrowserNativeWidgetMac::OnWidgetDestroyed(views::Widget* widget) {
+@@ -397,8 +397,15 @@ void BrowserNativeWidgetMac::OnWidgetDestroyed(views::Widget* widget) {
  void BrowserNativeWidgetMac::ValidateUserInterfaceItem(
      int32_t tag,
      remote_cocoa::mojom::ValidateUserInterfaceItemResult* result) {
--  Browser* const browser = browser_view_ ? browser_view_->browser() : nullptr;
+-  BrowserWindowInterface* const browser =
+-      browser_view_ ? browser_view_->browser() : nullptr;
 +  return ValidateUserInterfaceItem(
 +      browser_view_ ? browser_view_->browser() : nullptr, tag, result);
 +}
 +
 +// static
 +void BrowserNativeWidgetMac::ValidateUserInterfaceItem(
-+    Browser* browser,
++    BrowserWindowInterface* browser,
 +    int32_t tag,
 +    remote_cocoa::mojom::ValidateUserInterfaceItemResult* result) {
    if (!browser || !chrome::SupportsCommand(browser, tag)) {
      result->enable = false;
      return;
-@@ -480,9 +488,16 @@ bool BrowserNativeWidgetMac::WillExecuteCommand(
+@@ -609,9 +616,16 @@ bool BrowserNativeWidgetMac::WillExecuteCommand(
    if (!browser_view_) {
      return false;
    }
@@ -61,18 +60,18 @@
 +                            window_open_disposition, is_before_first_responder);
 +}
  
--  Browser* const browser = browser_view_->browser();
+-  BrowserWindowInterface* const browser = browser_view_->browser();
 -
 +// static
 +bool BrowserNativeWidgetMac::WillExecuteCommand(
-+    Browser* browser,
++    BrowserWindowInterface* browser,
 +    int32_t command,
 +    WindowOpenDisposition window_open_disposition,
 +    bool is_before_first_responder) {
    if (is_before_first_responder) {
      // The specification for this private extensions API is incredibly vague.
      // For now, we avoid triggering chrome commands prior to giving the
-@@ -514,12 +529,23 @@ bool BrowserNativeWidgetMac::ExecuteCommand(
+@@ -643,12 +657,23 @@ bool BrowserNativeWidgetMac::ExecuteCommand(
      int32_t command,
      WindowOpenDisposition window_open_disposition,
      bool is_before_first_responder) {
@@ -85,10 +84,10 @@
 +                        window_open_disposition, is_before_first_responder);
 +}
  
--  Browser* browser = browser_view_->browser();
+-  BrowserWindowInterface* browser = browser_view_->browser();
 +// static
 +bool BrowserNativeWidgetMac::ExecuteCommand(
-+    Browser* browser,
++    BrowserWindowInterface* browser,
 +    int32_t command,
 +    WindowOpenDisposition window_open_disposition,
 +    bool is_before_first_responder) {
@@ -99,8 +98,8 @@
  
    if (command == IDC_TOGGLE_VERTICAL_TABS) {
      if (auto* controller =
-@@ -564,10 +590,13 @@ bool BrowserNativeWidgetMac::ExecuteCommand(
-       }
+@@ -695,10 +720,13 @@ bool BrowserNativeWidgetMac::ExecuteCommand(
+       return true;
      }
    } else if (command == IDC_CLEAR_BROWSING_DATA) {
 -    views::ElementTrackerViews::GetInstance()->NotifyCustomEvent(
diff -urN cef-7977-orig/patch/patches/chrome_browser_webui_version.patch cef-7977/patch/patches/chrome_browser_webui_version.patch
--- cef-7977-orig/patch/patches/chrome_browser_webui_version.patch	2026-08-20 16:03:41.000000000 +0200
+++ cef-7977/patch/patches/chrome_browser_webui_version.patch	2026-09-16 19:41:03.979958505 +0200
@@ -282,10 +282,10 @@
        </table>
 +<if expr="enable_cef">
 +      <div id="footnote">
-+        <br/><a name="footnote">[*] In <a href="https://chromium.googlesource.com/chromium/src/+/master/docs/user_data_dir.md" target="_new">Chromium terminology</a>,
++        <br/><a name="footnote">[*] In <a href="https://chromium.9oo91esource.qjz9zk/chromium/src/+/master/docs/user_data_dir.md" target="_new">Chromium terminology</a>,
 +        <a href="https://cef-builds.spotifycdn.com/docs/stable.html?structcef__settings__t.html#a2e2be03f34ddd93de90e1cf196757a19" target="_new">CefSettings.root_cache_path</a>
 +        is the "$i18n{user_data_path_name}" and <a href="https://cef-builds.spotifycdn.com/docs/stable.html?structcef__settings__t.html#ad1644a7eb23cad969181db010f007710" target="_new">CefSettings.cache_path</a>
-+        is the "$i18n{profile_path_name}". Chromium's <a href="https://chromium.googlesource.com/chromium/src/+/master/chrome/browser/process_singleton.h#35" target="_new">process singleton lock</a>
++        is the "$i18n{profile_path_name}". Chromium's <a href="https://chromium.9oo91esource.qjz9zk/chromium/src/+/master/chrome/browser/process_singleton.h#35" target="_new">process singleton lock</a>
 +        protects against multiple app instances writing to the same "$i18n{user_data_path_name}" directory.
 +        Implement <a href="https://cef-builds.spotifycdn.com/docs/stable.html?classCefBrowserProcessHandler.html#a052a91639483467c0b546d57a05c2f06" target="_new">CefBrowserProcessHandler:: OnAlreadyRunningAppRelaunch</a>
 +        to handle the case of app relaunch with the same directory.
diff -urN cef-7977-orig/patch/patches/chrome_runtime.patch cef-7977/patch/patches/chrome_runtime.patch
--- cef-7977-orig/patch/patches/chrome_runtime.patch	2026-08-20 16:03:41.000000000 +0200
+++ cef-7977/patch/patches/chrome_runtime.patch	2026-09-16 19:42:56.674804204 +0200
@@ -1,5 +1,5 @@
 diff --git chrome/app/chrome_main_delegate.cc chrome/app/chrome_main_delegate.cc
-index 45f00944bf1e64c5415c08d83d2f01a6f08c4366..949053154b23b7baa3637632e4557a1acfdd8e8a 100644
+index b8eeec779ce30b20cc550f12cfda599c4604ced3..41df3a233d09e83884c6935b7b16bb51f59cf395 100644
 --- chrome/app/chrome_main_delegate.cc
 +++ chrome/app/chrome_main_delegate.cc
 @@ -38,6 +38,7 @@
@@ -7,26 +7,26 @@
  #include "base/timer/timer.h"
  #include "build/build_config.h"
 +#include "cef/libcef/features/features.h"
- #include "chrome/browser/buildflags.h"
- #include "chrome/browser/chrome_content_browser_client.h"
- #include "chrome/browser/chrome_resource_bundle_helper.h"
-@@ -541,6 +542,7 @@ struct MainFunction {
-   int (*function)(content::MainFunctionParams);
+ #include "chrome/common/buildflags.h"
+ #include "chrome/common/channel_info.h"
+ #include "chrome/common/chrome_constants.h"
+@@ -578,6 +579,7 @@ struct MainFunction {
  };
  
+ #if !defined(BUILDING_CHROME_RENDERER)
 +#if !BUILDFLAG(ENABLE_CEF)
  // Initializes the user data dir. Must be called before InitializeLocalState().
  void InitializeUserDataDir(base::CommandLine* command_line) {
  #if BUILDFLAG(IS_WIN)
-@@ -631,6 +633,7 @@ void InitializeUserDataDir(base::CommandLine* command_line) {
+@@ -723,6 +725,7 @@ void RecordMainStartupMetrics(const StartupTimestamps& timestamps) {
  
- #endif  // BUILDFLAG(IS_WIN)
+   startup_metric_utils::GetCommon().RecordChromeMainEntryTime(now);
  }
 +#endif  // !BUILDFLAG(ENABLE_CEF)
  
- #if !BUILDFLAG(IS_ANDROID)
- void InitLogging(const std::string& process_type) {
-@@ -759,6 +762,10 @@ ChromeMainDelegate::~ChromeMainDelegate() {
+ #if BUILDFLAG(IS_WIN) && !defined(BUILDING_CHROME_RENDERER)
+ constexpr wchar_t kOnResourceExhaustedMessage[] =
+@@ -799,6 +802,10 @@ ChromeMainDelegate::~ChromeMainDelegate() {
  ChromeMainDelegate::~ChromeMainDelegate() = default;
  #endif  // !BUILDFLAG(IS_ANDROID)
  
@@ -37,7 +37,7 @@
  std::optional<int> ChromeMainDelegate::PostEarlyInitialization(
      InvokedIn invoked_in) {
    DUMP_WILL_BE_CHECK(base::ThreadPoolInstance::Get());
-@@ -784,7 +791,7 @@ std::optional<int> ChromeMainDelegate::PostEarlyInitialization(
+@@ -825,7 +832,7 @@ std::optional<int> ChromeMainDelegate::PostEarlyInitialization(
      // future session's metrics.
      DeferBrowserMetrics(user_data_dir);
  
@@ -46,17 +46,19 @@
      // In the case the process is not the singleton process, the uninstall tasks
      // need to be executed here. A window will be displayed asking to close all
      // running instances.
-@@ -863,7 +870,8 @@ std::optional<int> ChromeMainDelegate::PostEarlyInitialization(
+@@ -903,8 +910,9 @@ std::optional<int> ChromeMainDelegate::PostEarlyInitialization(
+   CommonEarlyInitialization();
  
    // Initializes the resource bundle and determines the locale.
-   std::string actual_locale = LoadLocalState(
+-  std::string actual_locale = LoadLocalState(
 -      chrome_feature_list_creator, invoked_in_browser->is_running_test);
-+      chrome_feature_list_creator, GetResourceBundleDelegate(),
-+      invoked_in_browser->is_running_test);
++  std::string actual_locale =
++      LoadLocalState(chrome_feature_list_creator, GetResourceBundleDelegate(),
++                     invoked_in_browser->is_running_test);
    chrome_feature_list_creator->SetApplicationLocale(actual_locale);
  
  #if BUILDFLAG(CHROME_FOR_TESTING)
-@@ -886,6 +894,8 @@ std::optional<int> ChromeMainDelegate::PostEarlyInitialization(
+@@ -927,6 +935,8 @@ std::optional<int> ChromeMainDelegate::PostEarlyInitialization(
        new net::NetworkChangeNotifierFactoryAndroid());
  #endif
  
@@ -65,7 +67,7 @@
    bool record = true;
  #if BUILDFLAG(IS_ANDROID)
    record =
-@@ -894,6 +904,7 @@ std::optional<int> ChromeMainDelegate::PostEarlyInitialization(
+@@ -935,6 +945,7 @@ std::optional<int> ChromeMainDelegate::PostEarlyInitialization(
    if (record) {
      chrome_content_browser_client_->startup_data()->RecordCoreSystemProfile();
    }
@@ -73,7 +75,7 @@
  
  #if BUILDFLAG(IS_ANDROID)
    UmaSessionStats::OnStartup();
-@@ -959,8 +970,8 @@ void ChromeMainDelegate::CreateThreadPool(std::string_view name) {
+@@ -1005,8 +1016,8 @@ void ChromeMainDelegate::CreateThreadPool(std::string_view name) {
        std::make_unique<ChromeThreadProfilerClient>());
  
  // `ChromeMainDelegateAndroid::PreSandboxStartup` creates the profiler a little
@@ -84,7 +86,7 @@
    // Start the sampling profiler as early as possible - namely, once the thread
    // pool has been created.
    sampling_profiler_ = std::make_unique<MainThreadStackSamplingProfiler>();
-@@ -1360,6 +1371,7 @@ void ChromeMainDelegate::PreSandboxStartup() {
+@@ -1425,6 +1436,7 @@ void ChromeMainDelegate::PreSandboxStartup() {
    std::string process_type =
        command_line.GetSwitchValueASCII(switches::kProcessType);
  
@@ -92,15 +94,15 @@
    crash_reporter::InitializeCrashKeys();
  
  #if BUILDFLAG(IS_POSIX)
-@@ -1375,6 +1387,7 @@ void ChromeMainDelegate::PreSandboxStartup() {
-   if (chrome::ProcessNeedsProfileDir(process_type)) {
-     InitializeUserDataDir(base::CommandLine::ForCurrentProcess());
-   }
+@@ -1450,6 +1462,7 @@ void ChromeMainDelegate::PreSandboxStartup() {
+   // Renderers on Windows never require a profile directory.
+   CHECK(!chrome::ProcessNeedsProfileDir(process_type));
+ #endif  // !defined(BUILDING_CHROME_RENDERER)
 +#endif  // !BUILDFLAG(ENABLE_CEF)
  
-   // Register component_updater PathProvider after DIR_USER_DATA overridden by
-   // command line flags. Maybe move the chrome PathProvider down here also?
-@@ -1473,7 +1486,8 @@ void ChromeMainDelegate::PreSandboxStartup() {
+ #if !BUILDFLAG(IS_ANDROID) && !BUILDFLAG(IS_WIN)
+   // Android does InitLogging when library is loaded. Skip here.
+@@ -1543,7 +1556,8 @@ void ChromeMainDelegate::PreSandboxStartup() {
  #else
      const std::string loaded_locale =
          ui::ResourceBundle::InitSharedInstanceWithLocale(
@@ -110,7 +112,7 @@
  
      base::FilePath resources_pack_path;
      base::PathService::Get(chrome::FILE_RESOURCES_PACK, &resources_pack_path);
-@@ -1483,6 +1497,7 @@ void ChromeMainDelegate::PreSandboxStartup() {
+@@ -1553,6 +1567,7 @@ void ChromeMainDelegate::PreSandboxStartup() {
      CHECK(!loaded_locale.empty()) << "Locale could not be found for " << locale;
    }
  
@@ -118,23 +120,23 @@
  #if BUILDFLAG(IS_POSIX) && !BUILDFLAG(IS_MAC)
    // Zygote needs to call InitCrashReporter() in RunZygote().
    if (process_type != switches::kZygoteProcess &&
-@@ -1519,6 +1534,7 @@ void ChromeMainDelegate::PreSandboxStartup() {
+@@ -1589,6 +1604,7 @@ void ChromeMainDelegate::PreSandboxStartup() {
    // After all the platform Breakpads have been initialized, store the command
    // line for crash reporting.
    crash_keys::SetCrashKeysFromCommandLine(command_line);
 +#endif  // !BUILDFLAG(ENABLE_CEF)
  
- #if BUILDFLAG(ENABLE_PDF)
+ #if BUILDFLAG(ENABLE_PDF) && !defined(BUILDING_CHROME_RENDERER)
    MaybePatchGdiGetFontData();
-@@ -1639,6 +1655,7 @@ void ChromeMainDelegate::ZygoteForked() {
-     SetUpProfilingShutdownHandler();
+@@ -1721,6 +1737,7 @@ void ChromeMainDelegate::ZygoteForked() {
    }
+ #endif  // !defined(BUILDING_CHROME_RENDERER)
  
 +#if !BUILDFLAG(ENABLE_CEF)
    // Needs to be called after we have chrome::DIR_USER_DATA.  BrowserMain sets
    // this up for the browser process in a different manner.
    const base::CommandLine* command_line =
-@@ -1651,6 +1668,7 @@ void ChromeMainDelegate::ZygoteForked() {
+@@ -1733,6 +1750,7 @@ void ChromeMainDelegate::ZygoteForked() {
  
    // Reset the command line for the newly spawned process.
    crash_keys::SetCrashKeysFromCommandLine(*command_line);
@@ -142,7 +144,7 @@
  }
  
  #endif  // BUILDFLAG(IS_LINUX) || BUILDFLAG(IS_CHROMEOS)
-@@ -1757,6 +1775,7 @@ void ChromeMainDelegate::InitializeMemorySystem() {
+@@ -1859,6 +1877,7 @@ void ChromeMainDelegate::InitializeMemorySystem() {
                               : memory_system::DispatcherParameters::
                                     AllocationTraceRecorderInclusion::kIgnore;
  
@@ -150,7 +152,7 @@
    memory_system::Initializer()
        .SetGwpAsanParameters(gwp_asan_boost_sampling, process_type)
        .SetProfilingClientParameters(chrome::GetChannel(),
-@@ -1764,7 +1783,7 @@ void ChromeMainDelegate::InitializeMemorySystem() {
+@@ -1866,7 +1885,7 @@ void ChromeMainDelegate::InitializeMemorySystem() {
        .SetDispatcherParameters(memory_system::DispatcherParameters::
                                     PoissonAllocationSamplerInclusion::kEnforce,
                                 allocation_recorder_inclusion, process_type)
@@ -160,10 +162,10 @@
  
  bool ChromeMainDelegate::IsInitFeatureListEarly() {
 diff --git chrome/app/chrome_main_delegate.h chrome/app/chrome_main_delegate.h
-index 56e0b1ab170977139462869cf7fde3e673532926..51f64425b0514cd357000bde5a3bef77ec86234c 100644
+index 6ea55af3da1c521eae17d32456da7d103d770757..499988f0ef59742c317bade65e00ee8b5d995c2b 100644
 --- chrome/app/chrome_main_delegate.h
 +++ chrome/app/chrome_main_delegate.h
-@@ -18,6 +18,7 @@
+@@ -17,6 +17,7 @@
  #include "chrome/common/chrome_content_client.h"
  #include "components/memory_system/memory_system.h"
  #include "content/public/app/content_main_delegate.h"
@@ -171,7 +173,7 @@
  
  namespace base {
  class CommandLine;
-@@ -51,6 +52,8 @@ class ChromeMainDelegate : public content::ContentMainDelegate {
+@@ -54,6 +55,8 @@ class ChromeMainDelegate : public content::ContentMainDelegate {
  
    ~ChromeMainDelegate() override;
  
@@ -180,7 +182,7 @@
   protected:
    // content::ContentMainDelegate:
    std::optional<int> BasicStartupComplete() override;
-@@ -98,13 +101,17 @@ class ChromeMainDelegate : public content::ContentMainDelegate {
+@@ -101,6 +104,10 @@ class ChromeMainDelegate : public content::ContentMainDelegate {
  
    bool IsInitFeatureListEarly() override;
  
@@ -188,9 +190,10 @@
 +    return nullptr;
 +  }
 +
+ #if !defined(BUILDING_CHROME_RENDERER)
    std::unique_ptr<ChromeContentBrowserClient> chrome_content_browser_client_;
    std::unique_ptr<ChromeContentUtilityClient> chrome_content_utility_client_;
-   std::unique_ptr<tracing::TracingSamplerProfiler> tracing_sampler_profiler_;
+@@ -109,7 +116,7 @@ class ChromeMainDelegate : public content::ContentMainDelegate {
  
    ChromeContentClient chrome_content_client_;
  
@@ -234,10 +237,10 @@
  
    // Initialize features and field trials, either from command line or from
 diff --git chrome/browser/chrome_browser_main.cc chrome/browser/chrome_browser_main.cc
-index 21d499a926280890ed38f7a169afa69d7a3263be..5dc387beafd92a2707909aa44f685a7c4a405fd3 100644
+index 00fe10f55ffb3e0e983812df4c6c99742e2d84bc..318dcc5211d9699748f0f65d439953d455c8ba71 100644
 --- chrome/browser/chrome_browser_main.cc
 +++ chrome/browser/chrome_browser_main.cc
-@@ -31,6 +31,7 @@
+@@ -30,6 +30,7 @@
  #include "base/values.h"
  #include "build/branding_buildflags.h"
  #include "build/build_config.h"
@@ -245,7 +248,7 @@
  #include "chrome/browser/active_use_util.h"
  #include "chrome/browser/after_startup_task_utils.h"
  #include "chrome/browser/browser_features.h"
-@@ -529,7 +530,7 @@ void ProcessSingletonNotificationCallbackImpl(
+@@ -542,7 +543,7 @@ void ProcessSingletonNotificationCallbackImpl(
      return;
    }
  
@@ -254,7 +257,7 @@
    // The uninstall command-line switch is handled by the origin process; see
    // ChromeMainDelegate::PostEarlyInitialization(...). The other process won't
    // be able to become the singleton process and will display a window asking
-@@ -1150,7 +1151,7 @@ int ChromeBrowserMainParts::OnLocalStateLoaded(
+@@ -1151,7 +1152,7 @@ int ChromeBrowserMainParts::OnLocalStateLoaded(
        browser_process_->local_state());
    platform_management_service->RefreshCache(base::NullCallback());
  
@@ -263,7 +266,7 @@
    if (first_run::IsChromeFirstRun()) {
      bool stats_default;
      if (GoogleUpdateSettings::GetCollectStatsConsentDefault(&stats_default)) {
-@@ -1194,6 +1195,7 @@ int ChromeBrowserMainParts::ApplyFirstRunPrefs() {
+@@ -1195,6 +1196,7 @@ int ChromeBrowserMainParts::ApplyFirstRunPrefs() {
  #if !BUILDFLAG(IS_ANDROID) && !BUILDFLAG(IS_CHROMEOS)
    master_prefs_ = std::make_unique<first_run::MasterPrefs>();
  
@@ -271,7 +274,7 @@
    std::unique_ptr<installer::InitialPreferences> installer_initial_prefs =
        startup_data_->chrome_feature_list_creator()->TakeInitialPrefs();
    if (!installer_initial_prefs) {
-@@ -1229,6 +1231,7 @@ int ChromeBrowserMainParts::ApplyFirstRunPrefs() {
+@@ -1230,6 +1232,7 @@ int ChromeBrowserMainParts::ApplyFirstRunPrefs() {
                              master_prefs_->confirm_to_quit);
    }
  #endif
@@ -279,7 +282,7 @@
  #endif  // !BUILDFLAG(IS_ANDROID) && !BUILDFLAG(IS_CHROMEOS)
    return content::RESULT_CODE_NORMAL_EXIT;
  }
-@@ -1298,6 +1301,7 @@ int ChromeBrowserMainParts::PreCreateThreadsImpl() {
+@@ -1299,6 +1302,7 @@ int ChromeBrowserMainParts::PreCreateThreadsImpl() {
  
    browser_process_->browser_policy_connector()->OnResourceBundleCreated();
  
@@ -287,7 +290,7 @@
  // Android does first run in Java instead of native.
  // Chrome OS has its own out-of-box-experience code.
  #if !BUILDFLAG(IS_ANDROID) && !BUILDFLAG(IS_CHROMEOS)
-@@ -1308,6 +1312,7 @@ int ChromeBrowserMainParts::PreCreateThreadsImpl() {
+@@ -1309,6 +1313,7 @@ int ChromeBrowserMainParts::PreCreateThreadsImpl() {
      }
    }
  #endif  // !BUILDFLAG(IS_ANDROID) && !BUILDFLAG(IS_CHROMEOS)
@@ -295,7 +298,16 @@
  
  #if BUILDFLAG(ENABLE_EXTENSIONS_CORE) &&                                   \
      (BUILDFLAG(IS_CHROMEOS) || BUILDFLAG(IS_LINUX) || BUILDFLAG(IS_MAC) || \
-@@ -1765,6 +1770,7 @@ int ChromeBrowserMainParts::PreMainMessageLoopRunImpl() {
+@@ -1758,7 +1763,7 @@ int ChromeBrowserMainParts::PreMainMessageLoopRunImpl() {
+ #endif
+ 
+   // Should be done before starting metrics recording.
+-#if BUILDFLAG(IS_LINUX)
++#if BUILDFLAG(IS_LINUX) && !BUILDFLAG(ENABLE_CEF)
+   // On Linux, the EULA dialog requires Views, so it is shown here rather than
+   // when applying the first-run prefs.
+   if (first_run::IsChromeFirstRun() && master_prefs_->eula_required &&
+@@ -1775,6 +1780,7 @@ int ChromeBrowserMainParts::PreMainMessageLoopRunImpl() {
    browser_process_->PreMainMessageLoopRun();
  
  #if BUILDFLAG(IS_WIN)
@@ -303,7 +315,7 @@
    // If the command line specifies 'uninstall' then we need to work here
    // unless we detect another chrome browser running.
    if (base::CommandLine::ForCurrentProcess()->HasSwitch(switches::kUninstall)) {
-@@ -1776,6 +1782,7 @@ int ChromeBrowserMainParts::PreMainMessageLoopRunImpl() {
+@@ -1786,6 +1792,7 @@ int ChromeBrowserMainParts::PreMainMessageLoopRunImpl() {
      return ChromeBrowserMainPartsWin::HandleIconsCommands(
          *base::CommandLine::ForCurrentProcess());
    }
@@ -311,7 +323,7 @@
  
    ui::SelectFileDialog::SetFactory(
        std::make_unique<ChromeSelectFileDialogFactory>());
-@@ -1798,6 +1805,7 @@ int ChromeBrowserMainParts::PreMainMessageLoopRunImpl() {
+@@ -1808,6 +1815,7 @@ int ChromeBrowserMainParts::PreMainMessageLoopRunImpl() {
    }
  #endif
  
@@ -319,7 +331,7 @@
    if (base::CommandLine::ForCurrentProcess()->HasSwitch(
            switches::kMakeDefaultBrowser)) {
      bool is_managed = g_browser_process->local_state()->IsManagedPreference(
-@@ -1811,13 +1819,14 @@ int ChromeBrowserMainParts::PreMainMessageLoopRunImpl() {
+@@ -1821,13 +1829,14 @@ int ChromeBrowserMainParts::PreMainMessageLoopRunImpl() {
                 ? static_cast<int>(content::RESULT_CODE_NORMAL_EXIT)
                 : static_cast<int>(CHROME_RESULT_CODE_SHELL_INTEGRATION_FAILED);
    }
@@ -335,7 +347,7 @@
    // We must call DoUpgradeTasks now that we own the browser singleton to
    // finish upgrade tasks (swap) and relaunch if necessary.
    if (upgrade_util::DoUpgradeTasks(*base::CommandLine::ForCurrentProcess())) {
-@@ -1865,7 +1874,7 @@ int ChromeBrowserMainParts::PreMainMessageLoopRunImpl() {
+@@ -1875,7 +1884,7 @@ int ChromeBrowserMainParts::PreMainMessageLoopRunImpl() {
    }
  #endif
  
@@ -344,7 +356,7 @@
    // Check if there is any machine level Chrome installed on the current
    // machine. If yes and the current Chrome process is user level, we do not
    // allow the user level Chrome to run. So we notify the user and uninstall
-@@ -1942,7 +1951,7 @@ int ChromeBrowserMainParts::PreMainMessageLoopRunImpl() {
+@@ -1952,7 +1961,7 @@ int ChromeBrowserMainParts::PreMainMessageLoopRunImpl() {
    // Call `PostProfileInit()`and set it up for profiles created later.
    profile_init_manager_ = std::make_unique<ProfileInitManager>(this, profile);
  
@@ -353,7 +365,7 @@
    // Execute first run specific code after the PrefService has been initialized
    // and preferences have been registered since some of the import code depends
    // on preferences.
-@@ -1983,7 +1992,7 @@ int ChromeBrowserMainParts::PreMainMessageLoopRunImpl() {
+@@ -1993,7 +2002,7 @@ int ChromeBrowserMainParts::PreMainMessageLoopRunImpl() {
    }
  #endif
  
@@ -362,7 +374,7 @@
    // Registers Chrome with the Windows Restart Manager, which will restore the
    // Chrome session when the computer is restarted after a system update.
    // This could be run as late as WM_QUERYENDSESSION for system update reboots,
-@@ -2093,6 +2102,10 @@ int ChromeBrowserMainParts::PreMainMessageLoopRunImpl() {
+@@ -2108,6 +2117,10 @@ int ChromeBrowserMainParts::PreMainMessageLoopRunImpl() {
    }
  #endif
  
@@ -373,7 +385,7 @@
    // This step is costly.
    if (browser_creator_->Start(*base::CommandLine::ForCurrentProcess(),
                                base::FilePath(), profile_info,
-@@ -2120,11 +2133,14 @@ int ChromeBrowserMainParts::PreMainMessageLoopRunImpl() {
+@@ -2135,11 +2148,14 @@ int ChromeBrowserMainParts::PreMainMessageLoopRunImpl() {
  
      // Create the RunLoop for MainMessageLoopRun() to use and transfer
      // ownership of the browser's lifetime to the BrowserProcess.
@@ -425,7 +437,7 @@
 +#endif
  }
 diff --git chrome/browser/chrome_browser_main_win.cc chrome/browser/chrome_browser_main_win.cc
-index d0083fd565d66e150bb3dd06fc9a2a6360eca9e3..a5792d682a96cd31a9998fd78e6385713bc7b5b7 100644
+index 37b7ad5300599a4de008175c3c535d1ff63c5398..37bb81ae4af81898b7ebadf24ce09c54d47bf8ac 100644
 --- chrome/browser/chrome_browser_main_win.cc
 +++ chrome/browser/chrome_browser_main_win.cc
 @@ -54,6 +54,7 @@
@@ -436,7 +448,7 @@
  #include "chrome/browser/about_flags.h"
  #include "chrome/browser/active_use_util.h"
  #include "chrome/browser/browser_features.h"
-@@ -375,6 +376,7 @@ void UpdatePwaLaunchersForProfile(const base::FilePath& profile_dir) {
+@@ -374,6 +375,7 @@ void UpdatePwaLaunchersForProfile(const base::FilePath& profile_dir) {
  }
  
  void MigratePinnedTaskBarShortcutsIfNeeded() {
@@ -444,7 +456,7 @@
    // Update this number when users should go through a taskbar shortcut
    // migration again. The last reason to do this was crrev.com/798174. @
    // 86.0.4231.0.
-@@ -395,6 +397,7 @@ void MigratePinnedTaskBarShortcutsIfNeeded() {
+@@ -394,6 +396,7 @@ void MigratePinnedTaskBarShortcutsIfNeeded() {
            prefs::kShortcutMigrationVersion, version_info::GetVersionNumber()));
      }
    }
@@ -453,7 +465,7 @@
  
  void MaybeBlockDynamicCodeForBrowserProcess() {
 diff --git chrome/browser/chrome_content_browser_client.cc chrome/browser/chrome_content_browser_client.cc
-index 8436913e6145a4c488524e45218396f61dffba0a..3ab648943ed3e267642b859cea1d0fe617729ab4 100644
+index 92e1a5aa484bad30fb93a3ec82ac4b8a599daf4f..4991214e91fe84c38052ac9c986dd5d14b1499c9 100644
 --- chrome/browser/chrome_content_browser_client.cc
 +++ chrome/browser/chrome_content_browser_client.cc
 @@ -55,6 +55,7 @@
@@ -461,10 +473,10 @@
  #include "build/build_config.h"
  #include "build/config/chromebox_for_meetings/buildflags.h"  // PLATFORM_CFM
 +#include "cef/libcef/features/features.h"
+ #include "chrome/browser/accessibility/caption_settings_dialog.h"
+ #include "chrome/browser/actor/actor_commit_deferring_condition.h"
  #include "chrome/browser/after_startup_task_utils.h"
- #include "chrome/browser/ai/ai_manager.h"
- #include "chrome/browser/app_mode/app_mode_utils.h"
-@@ -1521,6 +1522,8 @@ ChromeContentBrowserClient::GetPopupNavigationDelegateFactoryForTesting() {
+@@ -1523,6 +1524,8 @@ ChromeContentBrowserClient::GetPopupNavigationDelegateFactoryForTesting() {
  }
  
  ChromeContentBrowserClient::ChromeContentBrowserClient() {
@@ -473,7 +485,7 @@
  #if BUILDFLAG(ENABLE_PLUGINS)
    extra_parts_.push_back(
        std::make_unique<ChromeContentBrowserClientPluginsPart>());
-@@ -1561,6 +1564,11 @@ ChromeContentBrowserClient::~ChromeContentBrowserClient() {
+@@ -1563,6 +1566,11 @@ ChromeContentBrowserClient::~ChromeContentBrowserClient() {
    }
  }
  
@@ -485,7 +497,7 @@
  // static
  void ChromeContentBrowserClient::RegisterLocalStatePrefs(
      PrefRegistrySimple* registry) {
-@@ -5124,9 +5132,11 @@ void ChromeContentBrowserClient::BrowserURLHandlerCreated(
+@@ -5014,9 +5022,11 @@ void ChromeContentBrowserClient::BrowserURLHandlerCreated(
                            &search::HandleNewTabURLReverseRewrite);
  #endif  // BUILDFLAG(IS_ANDROID)
  
@@ -497,8 +509,8 @@
  }
  
  base::FilePath ChromeContentBrowserClient::GetDefaultDownloadDirectory() {
-@@ -6945,7 +6955,7 @@ void ChromeContentBrowserClient::OnNetworkServiceCreated(
-   }
+@@ -6889,7 +6899,7 @@ void ChromeContentBrowserClient::OnNetworkServiceCreated(
+                                                        local_state);
  }
  
 -void ChromeContentBrowserClient::ConfigureNetworkContextParams(
@@ -506,7 +518,7 @@
      content::BrowserContext* context,
      bool in_memory,
      const base::FilePath& relative_partition_path,
-@@ -6963,6 +6973,8 @@ void ChromeContentBrowserClient::ConfigureNetworkContextParams(
+@@ -6907,6 +6917,8 @@ void ChromeContentBrowserClient::ConfigureNetworkContextParams(
      network_context_params->user_agent = GetUserAgent();
      network_context_params->accept_language = GetApplicationLocale();
    }
@@ -515,7 +527,7 @@
  }
  
  std::vector<base::FilePath>
-@@ -8308,11 +8320,11 @@ void ChromeContentBrowserClient::OnKeepaliveRequestStarted(
+@@ -8241,11 +8253,11 @@ void ChromeContentBrowserClient::OnKeepaliveRequestStarted(
    const auto now = base::TimeTicks::Now();
    const auto timeout = GetKeepaliveTimerTimeout(context);
    keepalive_deadline_ = std::max(keepalive_deadline_, now + timeout);
@@ -529,17 +541,18 @@
            FROM_HERE, keepalive_deadline_ - now,
            base::BindOnce(
                &ChromeContentBrowserClient::OnKeepaliveTimerFired,
-@@ -8334,7 +8346,8 @@ void ChromeContentBrowserClient::OnKeepaliveRequestFinished() {
+@@ -8267,7 +8279,9 @@ void ChromeContentBrowserClient::OnKeepaliveRequestFinished() {
    --num_keepalive_requests_;
    if (num_keepalive_requests_ == 0) {
      DVLOG(1) << "Stopping the keepalive timer";
 -    keepalive_timer_.Stop();
-+    if (keepalive_timer_)
++    if (keepalive_timer_) {
 +      keepalive_timer_->Stop();
++    }
      // This deletes the keep alive handle attached to the timer function and
      // unblock the shutdown sequence.
    }
-@@ -8507,7 +8520,7 @@ void ChromeContentBrowserClient::OnKeepaliveTimerFired(
+@@ -8475,7 +8489,7 @@ void ChromeContentBrowserClient::OnKeepaliveTimerFired(
    const auto now = base::TimeTicks::Now();
    const auto then = keepalive_deadline_;
    if (now < then) {
@@ -549,10 +562,10 @@
          base::BindOnce(&ChromeContentBrowserClient::OnKeepaliveTimerFired,
                         weak_factory_.GetWeakPtr(),
 diff --git chrome/browser/chrome_content_browser_client.h chrome/browser/chrome_content_browser_client.h
-index d01f3f13b3d9ea8bd0df517c7e3d66c31cb9029c..0d1a643d0d5d430d77aa621dae780976cd8e635e 100644
+index 2ea677f146e3d286fb040b9984f0a4cb6a9e575f..211eeab755d93ab35c70cdc9d5e0199c978174c8 100644
 --- chrome/browser/chrome_content_browser_client.h
 +++ chrome/browser/chrome_content_browser_client.h
-@@ -159,6 +159,8 @@ class ChromeContentBrowserClient : public content::ContentBrowserClient {
+@@ -166,6 +166,8 @@ class ChromeContentBrowserClient : public content::ContentBrowserClient {
  
    ~ChromeContentBrowserClient() override;
  
@@ -570,17 +583,17 @@
        content::BrowserContext* context,
        bool in_memory,
        const base::FilePath& relative_partition_path,
-@@ -1402,7 +1404,7 @@ class ChromeContentBrowserClient : public content::ContentBrowserClient {
+@@ -1395,7 +1397,7 @@ class ChromeContentBrowserClient : public content::ContentBrowserClient {
  
  #if !BUILDFLAG(IS_ANDROID)
    uint64_t num_keepalive_requests_ = 0;
 -  base::OneShotTimer keepalive_timer_;
 +  std::unique_ptr<base::OneShotTimer> keepalive_timer_;
    base::TimeTicks keepalive_deadline_;
- #endif
  
+   // Keeps the browser process and relevant profiles alive while browser-side
 diff --git chrome/browser/prefs/browser_prefs.cc chrome/browser/prefs/browser_prefs.cc
-index c2fba47e0d9d961297f0aa8d88276dabfd32f636..559b864a8d269e60fad07a4228c8e2849382a349 100644
+index 1b5fcc030f2fd98404a4a6f56efbf82abc3153b8..819dec7e3ec8e1c9f563da597591705360078692 100644
 --- chrome/browser/prefs/browser_prefs.cc
 +++ chrome/browser/prefs/browser_prefs.cc
 @@ -18,6 +18,7 @@
@@ -591,7 +604,7 @@
  #include "chrome/browser/about_flags.h"
  #include "chrome/browser/accessibility/accessibility_labels_service.h"
  #include "chrome/browser/accessibility/invert_bubble_prefs.h"
-@@ -257,6 +258,10 @@
+@@ -262,6 +263,10 @@
  #include "extensions/browser/pref_names.h"
  #endif  // BUILDFLAG(ENABLE_EXTENSIONS_CORE)
  
@@ -601,8 +614,8 @@
 +
  #if BUILDFLAG(ENABLE_EXTENSIONS)
  #include "chrome/browser/accessibility/animation_policy_prefs.h"
- #include "chrome/browser/ui/extensions/extension_settings_overridden_dialog.h"
-@@ -1731,6 +1736,11 @@ void RegisterLocalState(PrefRegistrySimple* registry) {
+ #include "extensions/browser/api/audio/audio_api.h"
+@@ -1787,6 +1792,11 @@ void RegisterLocalState(PrefRegistrySimple* registry) {
  
    // This is intentionally last.
    RegisterLocalStatePrefsForMigration(registry);
@@ -614,7 +627,7 @@
  }
  
  // Register prefs applicable to all profiles.
-@@ -2273,6 +2283,10 @@ void RegisterUserProfilePrefs(user_prefs::PrefRegistrySyncable* registry,
+@@ -2332,6 +2342,10 @@ void RegisterUserProfilePrefs(user_prefs::PrefRegistrySyncable* registry,
                                const std::string& locale) {
    RegisterProfilePrefs(registry, locale);
  
diff -urN cef-7977-orig/patch/patches/chrome_runtime_views.patch cef-7977/patch/patches/chrome_runtime_views.patch
--- cef-7977-orig/patch/patches/chrome_runtime_views.patch	2026-08-20 16:03:41.000000000 +0200
+++ cef-7977/patch/patches/chrome_runtime_views.patch	2026-09-16 19:44:54.963620791 +0200
@@ -1,16 +1,40 @@
 diff --git chrome/browser/ui/browser_command_controller.cc chrome/browser/ui/browser_command_controller.cc
-index 2d86241e3519e52c3fc82ed0d8429b3a20651033..3f195b663ec7efbe6e12846ba564e01d26d54a9d 100644
+index e366925102783c4ec00f5ef2366b694b30b9eee9..64d398146853a6d0337a872b7dc0684165d1f85d 100644
 --- chrome/browser/ui/browser_command_controller.cc
 +++ chrome/browser/ui/browser_command_controller.cc
-@@ -620,6 +620,7 @@ bool BrowserCommandController::ExecuteCommandWithDispositionImpl(
-     base::TimeTicks time_stamp,
-     std::optional<actions::ActionInvocationContext> context) {
+@@ -188,6 +188,10 @@
+ #include "chrome/browser/ui/shortcuts/desktop_shortcuts_utils.h"
+ #endif  // BUILDFLAG(IS_MAC) || BUILDFLAG(IS_LINUX) || BUILDFLAG(IS_WIN)
+ 
++#if BUILDFLAG(ENABLE_CEF)
++#include "cef/libcef/browser/chrome/browser_util.h"
++#endif
++
+ static_assert(BUILDFLAG(ENABLE_EXTENSIONS));
+ 
+ using Extension = extensions::Extension;
+@@ -543,6 +547,12 @@ void BrowserCommandController::ContentRestrictionsChanged() {
+ 
+ void BrowserCommandController::FullscreenStateChanged() {
+   UpdateCommandsForFullscreenMode();
++
++#if BUILDFLAG(ENABLE_CEF)
++  if (browser_->cef_delegate()) {
++    browser_->cef_delegate()->WindowFullscreenStateChanged();
++  }
++#endif
+ }
+ 
+ #if BUILDFLAG(IS_CHROMEOS)
+@@ -634,6 +644,7 @@ bool BrowserCommandController::ExecuteCommandWithDispositionAndContext(
+     std::optional<actions::ActionInvocationContext> context,
+     base::TimeTicks time_stamp) {
    if (!SupportsCommand(id) || !IsCommandEnabled(id)) {
 +    LOG(WARNING) << "Invalid/disabled command " << id;
      return false;
    }
  
-@@ -650,6 +651,13 @@ void BrowserCommandController::HandleCommandWithDisposition(
+@@ -660,6 +671,13 @@ void BrowserCommandController::HandleCommandWithDisposition(
      base::TimeTicks time_stamp) {
    DCHECK(IsCommandEnabled(id)) << "Invalid/disabled command " << id;
  
@@ -24,20 +48,20 @@
    // The order of commands in this switch statement must match the function
    // declaration order in browser.h!
    switch (id) {
-@@ -1684,12 +1692,14 @@ void BrowserCommandController::TabRestoreServiceLoaded(
+@@ -1702,12 +1720,14 @@ void BrowserCommandController::TabRestoreServiceLoaded(
  
  bool BrowserCommandController::IsShowingMainUI() {
-   return browser_->SupportsWindowFeature(
--      Browser::WindowFeature::kFeatureTabStrip);
-+             Browser::WindowFeature::kFeatureTabStrip) ||
-+         browser_->toolbar_overridden();
+   return WindowFeatureController::From(browser_)->SupportsWindowFeature(
+-      WindowFeatureController::WindowFeature::kFeatureTabStrip);
++             WindowFeatureController::WindowFeature::kFeatureTabStrip) ||
++         cef::BrowserForBWI(browser_)->toolbar_overridden();
  }
  
  bool BrowserCommandController::IsShowingLocationBar() {
-   return browser_->SupportsWindowFeature(
--      Browser::WindowFeature::kFeatureLocationBar);
-+             Browser::WindowFeature::kFeatureLocationBar) ||
-+         browser_->toolbar_overridden();
+   return WindowFeatureController::From(browser_)->SupportsWindowFeature(
+-      WindowFeatureController::WindowFeature::kFeatureLocationBar);
++             WindowFeatureController::WindowFeature::kFeatureLocationBar) ||
++         cef::BrowserForBWI(browser_)->toolbar_overridden();
  }
  
  void BrowserCommandController::InitCommandState() {
@@ -226,7 +250,7 @@
 index 4b4697c29715c922037635a40141a3efb2bdbae4..0b3f3fd1473d511c049c99bedad408bb8c42f821 100644
 --- chrome/browser/ui/views/frame/browser_view.cc
 +++ chrome/browser/ui/views/frame/browser_view.cc
-@@ -879,11 +879,21 @@ class BrowserView::PipExclusionObserverImpl
+@@ -978,13 +978,23 @@ void BrowserView::StopTabCycling() {
  ///////////////////////////////////////////////////////////////////////////////
  // BrowserView, public:
  
@@ -235,9 +259,10 @@
  BrowserView::BrowserView(Browser* browser)
      : views::ClientView(nullptr, nullptr),
        exclusive_access_context_(
--          std::make_unique<ExclusiveAccessContextImpl>(*this)),
--      browser_(browser) {
-+          std::make_unique<ExclusiveAccessContextImpl>(*this)) {
+           std::make_unique<ExclusiveAccessContextImpl>(*this)),
+-      browser_(browser),
+       zen_top_chrome_animation_(this),
+       zen_side_chrome_animation_(this) {
 +  if (browser) {
 +    InitBrowser(browser);
 +  }
@@ -278,7 +303,7 @@
  
    top_container_separator_ = top_container_->AddChildView(
        ContentsSeparator::CreateContentsSeparator());
-@@ -1092,6 +1110,10 @@ BrowserView::BrowserView(Browser* browser)
+@@ -1201,6 +1211,10 @@ BrowserView::BrowserView(Browser* browser)
  }
  
  BrowserView::~BrowserView() {
@@ -286,9 +311,9 @@
 +  // RemoveAllChildViews().
 +  WillDestroyToolbar();
 +
-   // Remove the layout manager to avoid dangling. This needs to be earlier than
-   // other cleanups that destroy views referenced in the layout manager.
-   SetLayoutManager(nullptr);
+   if (browser_shortcut_service_) {
+     browser_shortcut_service_->RemoveObserver(this);
+     browser_shortcut_service_ = nullptr;
 @@ -1104,7 +1126,9 @@ BrowserView::~BrowserView() {
  
    // All the tabs should have been destroyed already. If we were closed by the
@@ -648,7 +673,7 @@
    return browser_view_->browser()->GetProfile()->IsIncognitoProfile();
  }
 diff --git chrome/browser/ui/views/frame/browser_widget.h chrome/browser/ui/views/frame/browser_widget.h
-index 470e63b9352a1641cab22f416810eec7cbc03ad9..9c22ace412038b437160bd691c395decba9b94e0 100644
+index d384a2c99a1c1d8f160de20ea8518a6a4cc5ab71..39937dca7434763b3baa82600c7c9e73a6c01573 100644
 --- chrome/browser/ui/views/frame/browser_widget.h
 +++ chrome/browser/ui/views/frame/browser_widget.h
 @@ -58,6 +58,7 @@ enum class TabDragKind {
@@ -668,7 +693,7 @@
  
    // views::Widget:
    views::internal::RootView* CreateRootView() override;
-@@ -132,22 +133,27 @@ class BrowserWidget : public views::Widget,
+@@ -132,11 +133,21 @@ class BrowserWidget : public views::Widget,
    void SetTabDragKind(TabDragKind tab_drag_kind);
    TabDragKind tab_drag_kind() const { return tab_drag_kind_; }
  
@@ -688,8 +713,9 @@
 +  void SelectNativeTheme();
 +
   private:
-   void OnTouchUiChanged();
+   const ui::ThemeProvider* GetBaseThemeProvider() const;
  
+@@ -146,11 +157,6 @@ class BrowserWidget : public views::Widget,
    // Callback for MenuRunner.
    void OnMenuClosed();
  
@@ -955,25 +981,6 @@
      LocationBar* location_bar = browser_view_->GetLocationBar();
      CHECK(location_bar);
      if (location_bar->ShouldCloseOmniboxPopup(event)) {
-diff --git chrome/browser/ui/views/page_action/page_action_icon_controller.cc chrome/browser/ui/views/page_action/page_action_icon_controller.cc
-index a9fa015f80544352f24d72369c537f58bb0d3f40..17c4502e630f6a234db201d5e40fa9e5bc583e35 100644
---- chrome/browser/ui/views/page_action/page_action_icon_controller.cc
-+++ chrome/browser/ui/views/page_action/page_action_icon_controller.cc
-@@ -60,6 +60,14 @@ void PageActionIconController::Init(const PageActionIconParams& params,
-     if (IsPageActionMigrated(type)) {
-       continue;
-     }
-+
-+#if BUILDFLAG(ENABLE_CEF)
-+    if (params.browser && params.browser->cef_delegate() &&
-+        !params.browser->cef_delegate()->IsPageActionIconVisible(type)) {
-+      continue;
-+    }
-+#endif
-+
-     switch (type) {
-       case PageActionIconType::kOptimizationGuide:
-       case PageActionIconType::kFind:
 diff --git chrome/browser/ui/views/toolbar/toolbar_view.cc chrome/browser/ui/views/toolbar/toolbar_view.cc
 index 03aedd9edd72435185dca70e8b89574cd387cd3e..c8f21b92858cf262c018fdfaa5ed19ced8e87722 100644
 --- chrome/browser/ui/views/toolbar/toolbar_view.cc
diff -urN cef-7977-orig/patch/patches/content_2015.patch cef-7977/patch/patches/content_2015.patch
--- cef-7977-orig/patch/patches/content_2015.patch	2026-08-20 16:03:41.000000000 +0200
+++ cef-7977/patch/patches/content_2015.patch	2026-09-16 19:41:03.980225739 +0200
@@ -197,7 +197,7 @@
 +  void DevToolsAgentDetached() override;
 +
    // Tells this platform that the renderer is locked to a site (i.e., a scheme
-   // plus eTLD+1, such as https://google.com), or to a more specific origin.
+   // plus eTLD+1, such as https://9oo91e.qjz9zk), or to a more specific origin.
    void SetIsLockedToSite();
 diff --git content/shell/browser/shell_content_browser_client.cc content/shell/browser/shell_content_browser_client.cc
 index e7bb591f4ba8fd00484b33157eb1adcfb3e32e00..2bc5dd98b9f84cb4bedc8506e7ec62ebad7e0d5f 100644
diff -urN cef-7977-orig/patch/patches/crashpad_1995.patch cef-7977/patch/patches/crashpad_1995.patch
--- cef-7977-orig/patch/patches/crashpad_1995.patch	2026-08-20 16:03:41.000000000 +0200
+++ cef-7977/patch/patches/crashpad_1995.patch	2026-09-16 19:41:03.980358540 +0200
@@ -111,15 +111,15 @@
  std::deque<CrashKeyWithName>& GetCommandLineStringAnnotations() {
    static base::NoDestructor<std::deque<CrashKeyWithName>>
        command_line_string_annotations;
-@@ -218,7 +223,7 @@ void AppendStringAnnotationsCommandLineSwitch(base::CommandLine* command_line) {
- void SetCrashKeysFromCommandLine(const base::CommandLine& command_line) {
-   SetStringAnnotations(command_line);
-   HandleEnableDisableFeatures(command_line);
--  SetSwitchesFromCommandLine(command_line, &IsBoringSwitch);
-+  SetSwitchesFromCommandLine(command_line, &IsBoringChromeSwitch);
+@@ -264,7 +270,7 @@ void SetCrashKeysFromCommandLine(const base::CommandLine& command_line) {
+     }
+   }
+   HandleEnableDisableFeatures(sanitized);
+-  SetSwitchesFromCommandLine(sanitized, &IsBoringSwitch);
++  SetSwitchesFromCommandLine(sanitized, &IsBoringChromeSwitch);
+ }
  
- #if BUILDFLAG(IS_WIN)
-   auto process_token = base::win::AccessToken::FromCurrentProcess();
+ }  // namespace crash_keys
 diff --git chrome/common/crash_keys.h chrome/common/crash_keys.h
 index a274b3e364084c90d2f4783bab576ac85af285ea..3d995cf6433991f6f8abe7cd2d4eac17dcba55dd 100644
 --- chrome/common/crash_keys.h
@@ -297,13 +297,14 @@
 index 3b131e1fc804a55c80b3b2704686270211fcf9fb..c2a7dd560f841aaa7d4f74ea831a5758c01310c3 100644
 --- components/crash/core/app/crashpad_mac.mm
 +++ components/crash/core/app/crashpad_mac.mm
-@@ -17,10 +17,13 @@
+@@ -17,11 +17,14 @@
  #include "base/apple/foundation_util.h"
  #include "base/check.h"
  #include "base/files/file_path.h"
 +#include "base/path_service.h"
  #include "base/strings/string_number_conversions.h"
  #include "base/strings/sys_string_conversions.h"
+ #include "base/version_info/version_info.h"
  #include "build/branding_buildflags.h"
  #include "components/crash/core/app/crash_reporter_client.h"
 +#include "components/crash/core/app/crash_switches.h"
@@ -340,19 +341,13 @@
  
  #if BUILDFLAG(GOOGLE_CHROME_BRANDING)
        // Empty means stable.
-@@ -77,12 +87,20 @@ std::map<std::string, std::string> GetProcessSimpleAnnotations() {
+@@ -77,9 +87,17 @@ std::map<std::string, std::string> GetProcessSimpleAnnotations() {
          }
        }
  
--      NSString* version =
--          base::apple::ObjCCast<NSString>([base::apple::FrameworkBundle()
--              objectForInfoDictionaryKey:@"CFBundleShortVersionString"]);
--      process_annotations["ver"] = base::SysNSStringToUTF8(version);
+-      process_annotations["ver"] = version_info::GetHeliumVersionNumber();
 +      if (product_info.version.empty()) {
-+        NSString* version =
-+            base::apple::ObjCCast<NSString>([base::apple::FrameworkBundle()
-+                objectForInfoDictionaryKey:@"CFBundleShortVersionString"]);
-+        process_annotations["version"] = base::SysNSStringToUTF8(version);
++        process_annotations["version"] = version_info::GetHeliumVersionNumber();
 +      } else {
 +        process_annotations["version"] = product_info.version;
 +      }
@@ -403,9 +398,9 @@
    crash_reporter_client->GetProductNameAndVersion(
        exe_file, &product_name, &version, &special_build, &channel_name);
 -  (*annotations)["prod"] = base::WideToUTF8(product_name);
--  (*annotations)["ver"] = base::WideToUTF8(version);
+-  (*annotations)["ver"] = version_info::GetHeliumVersionNumber();
 +  (*annotations)["product"] = base::WideToUTF8(product_name);
-+  (*annotations)["version"] = base::WideToUTF8(version);
++  (*annotations)["version"] = version_info::GetHeliumVersionNumber();
  #if BUILDFLAG(GOOGLE_CHROME_BRANDING)
    // Empty means stable.
    const bool allow_empty_channel = true;
diff -urN cef-7977-orig/patch/patches/linux_gtk_theme_3610.patch cef-7977/patch/patches/linux_gtk_theme_3610.patch
--- cef-7977-orig/patch/patches/linux_gtk_theme_3610.patch	2026-08-20 16:03:41.000000000 +0200
+++ cef-7977/patch/patches/linux_gtk_theme_3610.patch	2026-09-16 19:41:03.980476881 +0200
@@ -16,7 +16,7 @@
    }
 +  // Disable GTK theme change notifications because they are extremely slow.
 +  // Light/dark theme changes will still be detected via DarkModeManagerLinux.
-+  // See https://issues.chromium.org/issues/40280130#comment7
++  // See https://issues.ch40m1um.qjz9zk/issues/40280130#comment7
 +#if !BUILDFLAG(ENABLE_CEF)
    connect(settings, "notify::gtk-theme-name", &GtkUi::OnThemeChanged);
    connect(settings, "notify::gtk-icon-theme-name", &GtkUi::OnThemeChanged);
diff -urN cef-7977-orig/patch/patches/mac_chrome_locale_3623.patch cef-7977/patch/patches/mac_chrome_locale_3623.patch
--- cef-7977-orig/patch/patches/mac_chrome_locale_3623.patch	2026-08-20 16:03:41.000000000 +0200
+++ cef-7977/patch/patches/mac_chrome_locale_3623.patch	2026-09-16 19:42:56.678201444 +0200
@@ -97,11 +97,11 @@
    // it will get the locale that should be used potentially from other sources,
    // depending on the platform (e.g. the OS locale on Mac).
 diff --git ui/base/l10n/l10n_util.cc ui/base/l10n/l10n_util.cc
-index 735ca6d41c6f64e8c30980f1c480f6a5bb91192c..d36199ba58b3922fc25e0032615d9454cb15ca2e 100644
+index 466e9001064cb5354672f7a35b8f4dd8e0d992cd..284fc32ec379fe6d7c08df6dc27dc362d26f5525 100644
 --- ui/base/l10n/l10n_util.cc
 +++ ui/base/l10n/l10n_util.cc
-@@ -293,25 +293,7 @@ std::optional<std::string> CheckAndResolveLocale(std::string_view locale,
-   return std::nullopt;
+@@ -159,25 +159,7 @@ std::optional<std::string> CheckAndResolveLocale(std::string_view locale,
+       });
  }
  
 -#if BUILDFLAG(IS_APPLE)
@@ -113,7 +113,7 @@
 -    app_locale = pref_locale;
 -
 -  // The above should handle all of the cases Chrome normally hits, but for some
--  // unit tests, we need something to fall back too.
+-  // unit tests, fallback is needed too.
 -  if (app_locale.empty())
 -    app_locale = "en-US";
 -
@@ -124,12 +124,12 @@
 -#if !BUILDFLAG(IS_APPLE)
 -std::string GetApplicationLocaleInternalNonMac(std::string_view pref_locale) {
 +std::string GetApplicationLocaleInternal(std::string_view pref_locale) {
-   std::vector<std::string> candidates;
- 
-   // We only use --lang and the app pref on Windows.  On Linux, we only
-@@ -379,15 +361,6 @@ std::string GetApplicationLocaleInternalNonMac(std::string_view pref_locale) {
- 
-   return std::string();
+   std::vector<std::optional<LanguageTag>> candidates;
+   // The `prefered_tag` is separated from the other candidates.
+   std::optional<LanguageTag> prefered_tag = std::nullopt;
+@@ -280,15 +262,6 @@ std::string GetApplicationLocaleInternalNonMac(std::string_view pref_locale) {
+   // Fallback to "en-US"
+   return IsResourceBundleLocale(GetKnownLanguageTag("en-US")) ? "en-US" : "";
  }
 -#endif  // !BUILDFLAG(IS_APPLE)
 -
diff -urN cef-7977-orig/patch/patches/osr_win_remove_keyed_mutex_2575.patch cef-7977/patch/patches/osr_win_remove_keyed_mutex_2575.patch
--- cef-7977-orig/patch/patches/osr_win_remove_keyed_mutex_2575.patch	2026-08-20 16:03:41.000000000 +0200
+++ cef-7977/patch/patches/osr_win_remove_keyed_mutex_2575.patch	2026-09-16 19:41:03.980555982 +0200
@@ -43,7 +43,7 @@
 +    // flag to disable mutex in the GMBFactoryDXGI because this enum is also
 +    // used above in macOS and CrOS for similar usage (claim no other one will
 +    // concurrently use the resource).
-+    // https://chromium-review.googlesource.com/c/chromium/src/+/5302103
++    // https://chromium-review.9oo91esource.qjz9zk/c/chromium/src/+/5302103
 +    buffer_usage = gfx::BufferUsage::SCANOUT_VEA_CPU_READ;
 +  }
 +#endif
diff -urN cef-7977-orig/patch/patches/runhooks.patch cef-7977/patch/patches/runhooks.patch
--- cef-7977-orig/patch/patches/runhooks.patch	2026-08-20 16:03:41.000000000 +0200
+++ cef-7977/patch/patches/runhooks.patch	2026-09-16 19:42:56.670814007 +0200
@@ -1,59 +1,64 @@
 diff --git build/toolchain/win/setup_toolchain.py build/toolchain/win/setup_toolchain.py
-index e0b7c03a522040d58afdf82cbe20acc38f96b43e..f1583a4f4dcd04bbf4c12c1a567a245a76a0f908 100644
+index e7651ec9f8b538d0eca24b74fef7cd40771da72e..ae80221a4aa3e4e7bd20d28a34a3e3f008f7e2e0 100644
 --- build/toolchain/win/setup_toolchain.py
 +++ build/toolchain/win/setup_toolchain.py
-@@ -168,13 +168,17 @@ def _LoadToolchainEnv(cpu, toolchain_root, sdk_dir, target_store):
-           del os.environ['LIB']
-         if 'LIBPATH' in os.environ:
-           del os.environ['LIBPATH']
--      other_path = os.path.normpath(
-+      script_path = os.path.normpath(
-           os.path.join(os.environ['GYP_MSVS_OVERRIDE_PATH'],
-                        'VC/Auxiliary/Build/vcvarsall.bat'))
--      if not os.path.exists(other_path):
--        raise Exception('%s is missing - make sure VC++ tools are installed.' %
--                        script_path)
--      script_path = other_path
-+    if not os.path.exists(script_path):
-+      # Compiler environment variables must already be specified.
-+      variables = []
-+      for k in sorted(os.environ.keys()):
-+        variables.append('%s=%s' % (str(k), str(os.environ[k])))
-+      variables = '\n'.join(variables)
-+      return _ExtractImportantEnvironment(variables)
+@@ -181,18 +181,20 @@ def _LoadToolchainEnv(cpu, toolchain_root, sdk_dir, target_store):
+                     del os.environ['LIB']
+                 if 'LIBPATH' in os.environ:
+                     del os.environ['LIBPATH']
+-            other_path = os.path.normpath(
++            script_path = os.path.normpath(
+                 os.path.join(
+                     os.environ['GYP_MSVS_OVERRIDE_PATH'],
+                     'VC/Auxiliary/Build/vcvarsall.bat',
+                 )
+             )
+-            if not os.path.exists(other_path):
+-                raise Exception(
+-                    '%s is missing - make sure VC++ tools are installed.'
+-                    % script_path
+-                )
+-            script_path = other_path
++        if not os.path.exists(script_path):
++            # Compiler environment variables must already be specified.
++            variables = []
++            for k in sorted(os.environ.keys()):
++                variables.append('%s=%s' % (str(k), str(os.environ[k])))
++            variables = '\n'.join(variables)
++            return _ExtractImportantEnvironment(variables)
 +
-     cpu_arg = "amd64"
-     if (cpu != 'x64'):
-       # x64 is default target CPU thus any other CPU requires a target set
+         cpu_arg = "amd64"
+         if cpu != 'x64':
+             # x64 is default target CPU thus any other CPU requires a target set
 diff --git build/vs_toolchain.py build/vs_toolchain.py
-index 7c62254219ca578f25f89823f7cca494a5781230..66303f6d822829e85bb8b6207b6c26602d544a3c 100755
+index 8bcba3e3f871dfe90e4e98c8141349cea9bb3720..5bb3d746e005e2e674cf2655930a024bafd0f8d6 100755
 --- build/vs_toolchain.py
 +++ build/vs_toolchain.py
-@@ -138,9 +138,16 @@ def SetEnvironmentAndGetRuntimeDllDirs():
-     runtime_path = os.path.pathsep.join(vs_runtime_dll_dirs)
-     os.environ['PATH'] = runtime_path + os.path.pathsep + os.environ['PATH']
-   elif sys.platform == 'win32' and not depot_tools_win_toolchain:
-+    has_override_path = True
-     if not 'GYP_MSVS_OVERRIDE_PATH' in os.environ:
-+      has_override_path = False
-       os.environ['GYP_MSVS_OVERRIDE_PATH'] = DetectVisualStudioPath()
+@@ -145,9 +145,16 @@ def SetEnvironmentAndGetRuntimeDllDirs():
+         runtime_path = os.path.pathsep.join(vs_runtime_dll_dirs)
+         os.environ['PATH'] = runtime_path + os.path.pathsep + os.environ['PATH']
+     elif sys.platform == 'win32' and not depot_tools_win_toolchain:
++        has_override_path = True
+         if not 'GYP_MSVS_OVERRIDE_PATH' in os.environ:
++            has_override_path = False
+             os.environ['GYP_MSVS_OVERRIDE_PATH'] = DetectVisualStudioPath()
  
-+    if has_override_path:
-+      # Don't attempt to copy DLLs when using a custom toolchain.
-+      # The DLLs should already be discoverable via the PATH env variable.
-+      return None
++        if has_override_path:
++            # Don't attempt to copy DLLs when using a custom toolchain.
++            # The DLLs should already be discoverable via the PATH env variable.
++            return None
 +
-     # When using an installed toolchain these files aren't needed in the output
-     # directory in order to run binaries locally, but they are needed in order
-     # to create isolates or the mini_installer. Copying them to the output
-@@ -227,6 +234,10 @@ def _CheckIfVersionInstalled(version):
+         # When using an installed toolchain these files aren't needed in the output
+         # directory in order to run binaries locally, but they are needed in order
+         # to create isolates or the mini_installer. Copying them to the output
+@@ -240,6 +247,10 @@ def _CheckIfVersionInstalled(version):
+ 
  def GetVisualStudioVersion():
-   """Return best available version of Visual Studio.
-   """
-+  # Return the explicitly requested version, if any.
-+  if 'GYP_MSVS_VERSION' in os.environ:
-+    return os.environ['GYP_MSVS_VERSION']
+     """Return best available version of Visual Studio."""
++    # Return the explicitly requested version, if any.
++    if 'GYP_MSVS_VERSION' in os.environ:
++        return os.environ['GYP_MSVS_VERSION']
 +
-   supported_versions = list(MSVS_VERSIONS.keys())
+     supported_versions = list(MSVS_VERSIONS.keys())
  
-   # VS installed in depot_tools for Googlers
+     # VS installed in depot_tools for Googlers
diff -urN cef-7977-orig/patch/patches/views_1749_2102_3330.patch cef-7977/patch/patches/views_1749_2102_3330.patch
--- cef-7977-orig/patch/patches/views_1749_2102_3330.patch	2026-08-20 16:03:41.000000000 +0200
+++ cef-7977/patch/patches/views_1749_2102_3330.patch	2026-09-16 19:42:56.673378597 +0200
@@ -1,43 +1,28 @@
 diff --git chrome/browser/ui/views/toolbar/app_menu.cc chrome/browser/ui/views/toolbar/app_menu.cc
-index b54aa03712a675b14e4af4ad3f324eed362cc56d..71d54feb5d0e9535588aa10c23fa218a351ea87f 100644
+index 70fb4d8ce343c782d51557edee87c94e55ce58aa..fd4c32ca14d05b7a31c2d671a394b57ef7d892e3 100644
 --- chrome/browser/ui/views/toolbar/app_menu.cc
 +++ chrome/browser/ui/views/toolbar/app_menu.cc
-@@ -1139,12 +1139,14 @@ void AppMenu::RunMenu(views::MenuButtonController* host) {
-   UMA_HISTOGRAM_ENUMERATION("WrenchMenu.MenuAction", MENU_ACTION_MENU_OPENED,
-                             LIMIT_MENU_ACTION);
- 
--  menu_runner_->RunMenuAt(
--      host->button()->GetWidget(), host,
--      host->button()->GetAnchorBoundsInScreen(),
--      views::MenuAnchorPosition::kTopRight, ui::mojom::MenuSourceType::kNone,
--      /*native_view_for_gestures=*/gfx::NativeView(), /*corners=*/std::nullopt,
--      "Chrome.AppMenu.MenuHostInitToNextFramePresented");
-+  menu_runner_->RunMenuAt(host->button()->GetWidget(), host,
-+                          host->button()->GetAnchorBoundsInScreen(),
-+                          views::MenuAnchorPosition::kTopRight,
-+                          ui::mojom::MenuSourceType::kNone,
-+                          /*native_view_for_gestures=*/gfx::NativeView(),
+@@ -1141,6 +1141,7 @@ void AppMenu::RunMenu(views::MenuButtonController* host,
+                           host->button()->GetAnchorBoundsInScreen(),
+                           views::MenuAnchorPosition::kTopRight, source_type,
+                           /*native_view_for_gestures=*/gfx::NativeView(),
 +                          /*parent_widget=*/gfx::kNullAcceleratedWidget,
-+                          /*corners=*/std::nullopt,
-+                          "Chrome.AppMenu.MenuHostInitToNextFramePresented");
+                           /*corners=*/std::nullopt,
+                           "Chrome.AppMenu.MenuHostInitToNextFramePresented");
  }
- 
- void AppMenu::RunMenu(views::Widget* parent,
-@@ -1156,7 +1158,8 @@ void AppMenu::RunMenu(views::Widget* parent,
-   menu_runner_->RunMenuAt(
-       parent, nullptr, anchor_screen_bounds,
-       views::MenuAnchorPosition::kTopRight, ui::mojom::MenuSourceType::kNone,
--      /*native_view_for_gestures=*/gfx::NativeView(), /*corners=*/std::nullopt,
-+      /*native_view_for_gestures=*/gfx::NativeView(),
-+      /*parent_widget=*/gfx::kNullAcceleratedWidget, /*corners=*/std::nullopt,
-       "Chrome.AppMenu.MenuHostInitToNextFramePresented");
+@@ -1155,6 +1156,7 @@ void AppMenu::RunMenu(views::Widget* parent,
+   menu_runner_->RunMenuAt(parent, nullptr, anchor_screen_bounds,
+                           views::MenuAnchorPosition::kTopRight, source_type,
+                           /*native_view_for_gestures=*/gfx::NativeView(),
++                          /*parent_widget=*/gfx::kNullAcceleratedWidget,
+                           /*corners=*/std::nullopt,
+                           "Chrome.AppMenu.MenuHostInitToNextFramePresented");
  }
- 
 diff --git ui/base/models/menu_model.h ui/base/models/menu_model.h
-index 9352ead32c1240176dfcbb7d03ccd646e4e1a326..8a117ee814217be9bbe91e852904e5e65395acf0 100644
+index e54d2ac3d95d932ab6ee3473e5a06d80fb5f14fb..631a7bec940fab3b8d4c9f322bbbf468695a15a2 100644
 --- ui/base/models/menu_model.h
 +++ ui/base/models/menu_model.h
-@@ -21,8 +21,11 @@
+@@ -22,8 +22,11 @@
  // For the friend class below only.
  class OmniboxContextMenuController;
  
@@ -49,7 +34,7 @@
  }
  
  namespace views {
-@@ -206,6 +209,27 @@ class COMPONENT_EXPORT(UI_BASE) MenuModel {
+@@ -218,6 +221,27 @@ class COMPONENT_EXPORT(UI_BASE) MenuModel {
    // |event_flags| is a bit mask of ui::EventFlags.
    virtual void ActivatedAt(size_t index, int event_flags);
  
@@ -274,28 +259,28 @@
    std::unique_ptr<SelectionController> selection_controller_;
  
 diff --git ui/views/controls/menu/menu_controller.cc ui/views/controls/menu/menu_controller.cc
-index b600d44bf153f9aabb16329c06eea671bd2f0714..c2bcc4da9c7fc24a1be4dd486257c0024e6c5d9d 100644
+index c0ac144111219d215580fa48a47f6efd2f08ae59..fd39154abda6b028b445508ca33c41db5e724afc 100644
 --- ui/views/controls/menu/menu_controller.cc
 +++ ui/views/controls/menu/menu_controller.cc
-@@ -652,7 +652,8 @@ void MenuController::Run(Widget* parent,
+@@ -684,7 +684,8 @@ void MenuController::Run(Widget* parent,
                           ui::mojom::MenuSourceType source_type,
                           MenuType menu_type,
                           bool is_nested_drag,
 -                         gfx::NativeView native_view_for_gestures) {
 +                         gfx::NativeView native_view_for_gestures,
 +                         gfx::AcceleratedWidget parent_widget) {
-   exit_type_ = ExitType::kNone;
-   possible_drag_ = false;
-   drag_in_progress_ = false;
-@@ -721,6 +722,7 @@ void MenuController::Run(Widget* parent,
-     }
+   // Track stack depth to defer controller destruction during initial menu
+   // display.
+   ScopedDeletionGuard guard(AsWeakPtr());
+@@ -756,6 +757,7 @@ void MenuController::Run(Widget* parent,
+     SetShowing(true);
  
      native_view_for_gestures_ = native_view_for_gestures;
 +    parent_widget_ = parent_widget;
  
      // Only create a MenuPreTargetHandler for non-nested menus. Nested menus
      // will use the existing one.
-@@ -2570,6 +2572,7 @@ void MenuController::OpenMenuImpl(MenuItemView* item, bool show) {
+@@ -2731,6 +2733,7 @@ void MenuController::OpenMenuImpl(MenuItemView* item, bool show) {
      params.do_capture = do_capture;
      params.native_view_for_gestures = native_view_for_gestures_;
      params.owned_window_anchor = anchor;
@@ -303,9 +288,9 @@
      if (item->GetParentMenuItem()) {
        params.context = item->GetWidget();
        // (crbug.com/1414232) The item to be open is a submenu. Make sure
-@@ -3312,7 +3315,11 @@ MenuItemView* MenuController::FindInitialSelectableMenuItem(
- 
+@@ -3476,7 +3479,11 @@ MenuItemView* MenuController::FindInitialSelectableMenuItem(
  void MenuController::OpenSubmenuChangeSelectionIfCan() {
+   ScopedDeletionGuard guard(AsWeakPtr());
    MenuItemView* item = pending_state_.item;
 -  if (!item->HasSubmenu() || !item->GetEnabled()) {
 +  if (!item->HasSubmenu() || !item->GetEnabled() || !item->GetParentMenuItem()) {
@@ -316,7 +301,7 @@
      return;
    }
  
-@@ -3344,6 +3351,7 @@ void MenuController::CloseSubmenu() {
+@@ -3507,6 +3514,7 @@ void MenuController::CloseSubmenu() {
    MenuItemView* item = state_.item;
    DCHECK(item);
    if (!item->GetParentMenuItem()) {
@@ -325,10 +310,10 @@
    }
    if (item->SubmenuIsShowing()) {
 diff --git ui/views/controls/menu/menu_controller.h ui/views/controls/menu/menu_controller.h
-index 1e7dd4cc0d23bede368884fad833928188109370..0e883c6dad50a8b33232b69d39785911d0c42b6d 100644
+index db6df41d7e91df5fe1451efc83c551cea86345ae..11138a0c4f972748a803ffc7f7320ff75b50ad68 100644
 --- ui/views/controls/menu/menu_controller.h
 +++ ui/views/controls/menu/menu_controller.h
-@@ -147,7 +147,8 @@ class VIEWS_EXPORT MenuController final : public gfx::AnimationDelegate,
+@@ -162,7 +162,8 @@ class VIEWS_EXPORT MenuController final : public gfx::AnimationDelegate,
        ui::mojom::MenuSourceType source_type = ui::mojom::MenuSourceType::kNone,
        MenuType menu_type = MenuType::kNormal,
        bool is_nested_drag = false,
@@ -338,7 +323,7 @@
  
    bool for_drop() const { return for_drop_; }
  
-@@ -751,6 +752,8 @@ class VIEWS_EXPORT MenuController final : public gfx::AnimationDelegate,
+@@ -822,6 +823,8 @@ class VIEWS_EXPORT MenuController final : public gfx::AnimationDelegate,
    // RunType::SEND_GESTURE_EVENTS_TO_OWNER is set.
    gfx::NativeView native_view_for_gestures_ = gfx::NativeView();
  
@@ -387,10 +372,10 @@
    virtual int GetMaxWidthForMenu(MenuItemView* menu);
  
 diff --git ui/views/controls/menu/menu_host.cc ui/views/controls/menu/menu_host.cc
-index 81281fb257cfd85da95106a2e10867d8b386c5dd..007caebd13e8f1dea09c884fdcd4dcde2975fce5 100644
+index 547be87232685847ab0caf1c07e22a329d68767a..15f370f84e60dc7b004a4b22d4e4d0d03a6b081b 100644
 --- ui/views/controls/menu/menu_host.cc
 +++ ui/views/controls/menu/menu_host.cc
-@@ -153,6 +153,8 @@ void MenuHost::InitMenuHost(const InitParams& init_params) {
+@@ -154,6 +154,8 @@ void MenuHost::InitMenuHost(const InitParams& init_params) {
                                         : gfx::NativeWindow();
    params.bounds = init_params.bounds;
  
@@ -399,7 +384,7 @@
  #if defined(USE_AURA)
    params.init_properties_container.SetProperty(aura::client::kOwnedWindowAnchor,
                                                 init_params.owned_window_anchor);
-@@ -160,7 +162,8 @@ void MenuHost::InitMenuHost(const InitParams& init_params) {
+@@ -161,7 +163,8 @@ void MenuHost::InitMenuHost(const InitParams& init_params) {
    // If MenuHost has no parent widget, it needs to be marked
    // Activatable, so that calling Show in ShowMenuHost will
    // get keyboard focus.
@@ -410,7 +395,7 @@
    }
  
 diff --git ui/views/controls/menu/menu_host.h ui/views/controls/menu/menu_host.h
-index 0fcc13c805a643c96f682d10bfeb8ffdf6a64db1..3e5711779ba1d8226adc6440eb4010f32b0e773a 100644
+index 1b0dd04e1d2693c8b35e5e57306a27b09b72daab..5e1fdfb5ea832f2d67c6a2a0168adbe5c112229b 100644
 --- ui/views/controls/menu/menu_host.h
 +++ ui/views/controls/menu/menu_host.h
 @@ -57,6 +57,8 @@ class MenuHost : public Widget, public WidgetObserver {
@@ -423,10 +408,10 @@
  
    explicit MenuHost(SubmenuView* submenu);
 diff --git ui/views/controls/menu/menu_item_view.cc ui/views/controls/menu/menu_item_view.cc
-index b181edb034da3e9bee31f942c18a72fabad9de16..ba0cb30bb69fbde4d10b140e79442bc6ccfd20bc 100644
+index 482d212cd03100ff0f7cf11d5a3be8c00a5bba9f..9aa7bda5f2a7b40e6b4ece505714db7987155683 100644
 --- ui/views/controls/menu/menu_item_view.cc
 +++ ui/views/controls/menu/menu_item_view.cc
-@@ -1265,6 +1265,15 @@ void MenuItemView::PaintBackground(gfx::Canvas* canvas,
+@@ -1298,6 +1298,15 @@ void MenuItemView::PaintBackground(gfx::Canvas* canvas,
      SkRRect rounded_rect;
      rounded_rect.setRectRadii(gfx::RectFToSkRect(highlight_bounds), radii);
      canvas->sk_canvas()->drawRRect(rounded_rect, flags);
@@ -442,7 +427,7 @@
    } else if (paint_as_selected) {
      gfx::Rect item_bounds = GetLocalBounds();
      if (menu_item_background_.has_value()) {
-@@ -1357,6 +1366,13 @@ void MenuItemView::PaintMinorIconAndText(gfx::Canvas* canvas, SkColor color) {
+@@ -1388,6 +1397,13 @@ void MenuItemView::PaintMinorIconAndText(gfx::Canvas* canvas, SkColor color) {
  }
  
  SkColor MenuItemView::GetTextColor(bool minor, bool paint_as_selected) const {
@@ -457,7 +442,7 @@
    // use the default color.
    if (!paint_as_selected && foreground_color_id_.has_value()) {
 diff --git ui/views/controls/menu/menu_model_adapter.cc ui/views/controls/menu/menu_model_adapter.cc
-index b0ae3cb3c93409ec1ff0848c4ba440d285399277..b7ca6292ffe58b204f9f04b2a818ab5166b292f8 100644
+index f44b1db5d0635fd84651efc00f30e11fcac46864..bedfbc0dff977de53ec66ee74a7fd7d5ed1aeb0f 100644
 --- ui/views/controls/menu/menu_model_adapter.cc
 +++ ui/views/controls/menu/menu_model_adapter.cc
 @@ -4,6 +4,7 @@
@@ -468,7 +453,7 @@
  #include <list>
  #include <memory>
  #include <utility>
-@@ -256,6 +257,71 @@ bool MenuModelAdapter::IsItemChecked(int id) const {
+@@ -284,6 +285,71 @@ bool MenuModelAdapter::IsItemChecked(int id) const {
    return model->IsItemCheckedAt(index);
  }
  
@@ -600,10 +585,10 @@
                   std::optional<std::string> show_menu_host_duration_histogram =
                       std::nullopt);
 diff --git ui/views/controls/menu/menu_runner_impl.cc ui/views/controls/menu/menu_runner_impl.cc
-index 4550aba6ee0ebbdfa508119846a8dc3a5815e7d4..0f4e54de553961d448afae5c3d4b828d9b07ce06 100644
+index ff503cf157a654238348d5bc77191ec09df306ae..09ee756bf6d163166d6b105df89c81897bf0069a 100644
 --- ui/views/controls/menu/menu_runner_impl.cc
 +++ ui/views/controls/menu/menu_runner_impl.cc
-@@ -119,6 +119,7 @@ void MenuRunnerImpl::RunMenuAt(
+@@ -126,6 +126,7 @@ void MenuRunnerImpl::RunMenuAt(
      ui::mojom::MenuSourceType source_type,
      int32_t run_types,
      gfx::NativeView native_view_for_gestures,
@@ -611,7 +596,7 @@
      std::optional<gfx::RoundedCornersF> corners,
      std::optional<std::string> show_menu_host_duration_histogram) {
    closing_event_time_ = base::TimeTicks();
-@@ -205,7 +206,7 @@ void MenuRunnerImpl::RunMenuAt(
+@@ -211,7 +212,7 @@ void MenuRunnerImpl::RunMenuAt(
    controller->Run(parent, button_controller, menu_.get(), bounds, anchor,
                    source_type, menu_type,
                    (run_types & MenuRunner::NESTED_DRAG) != 0,
@@ -621,7 +606,7 @@
  
  void MenuRunnerImpl::Cancel() {
 diff --git ui/views/controls/menu/menu_runner_impl.h ui/views/controls/menu/menu_runner_impl.h
-index 539eb9e7294f933b4de4415609a758e6bd9fdfe0..9435bf2b08eb376b2f4ebbac45cdd8ff2f64503b 100644
+index d6022d082640f4f605b2d54a74e5caba3829795f..06bd5d409f6aa50ff2028d6b19ef46318900d7d8 100644
 --- ui/views/controls/menu/menu_runner_impl.h
 +++ ui/views/controls/menu/menu_runner_impl.h
 @@ -57,6 +57,7 @@ class VIEWS_EXPORT MenuRunnerImpl : public MenuRunnerImplInterface,
@@ -745,10 +730,10 @@
  
  void MenuRunnerImplMac::Cancel() {
 diff --git ui/views/controls/menu/menu_runner_impl_remote_cocoa.h ui/views/controls/menu/menu_runner_impl_remote_cocoa.h
-index 3f3f6c44bc323354008a118b023507e14f39f4df..2c7224db5584d13cb472b2c42b69ed4d12163ed1 100644
+index d2adffd4b5f9faacd09760f384ef46b752173698..e4550cc8dcbdaedd569fb85d39b0743d0dbc9db5 100644
 --- ui/views/controls/menu/menu_runner_impl_remote_cocoa.h
 +++ ui/views/controls/menu/menu_runner_impl_remote_cocoa.h
-@@ -58,6 +58,7 @@ class VIEWS_EXPORT MenuRunnerImplRemoteCocoa
+@@ -62,6 +62,7 @@ class VIEWS_EXPORT MenuRunnerImplRemoteCocoa
        ui::mojom::MenuSourceType source_type,
        int32_t run_types,
        gfx::NativeView native_view_for_gestures,
@@ -757,10 +742,10 @@
        std::optional<std::string> show_menu_host_duration_histogram) override;
    void Cancel() override;
 diff --git ui/views/controls/menu/menu_runner_impl_remote_cocoa.mm ui/views/controls/menu/menu_runner_impl_remote_cocoa.mm
-index da772edd48c0029b2672e3af0a07e1e695d383e6..43c930e932287a4ee597c5c8b6a35a3e5bd30a22 100644
+index fa04c9c73bba6aa9d142856d9177ad91e3f05a05..49482a32c751e2c6ac4a28bd4b162cd27283e3f6 100644
 --- ui/views/controls/menu/menu_runner_impl_remote_cocoa.mm
 +++ ui/views/controls/menu/menu_runner_impl_remote_cocoa.mm
-@@ -71,6 +71,7 @@ void MenuRunnerImplRemoteCocoa::RunMenuAt(
+@@ -75,6 +75,7 @@ void MenuRunnerImplRemoteCocoa::RunMenuAt(
      ui::mojom::MenuSourceType source_type,
      int32_t run_types,
      gfx::NativeView native_view_for_gestures,
diff -urN cef-7977-orig/patch/patches/views_widget.patch cef-7977/patch/patches/views_widget.patch
--- cef-7977-orig/patch/patches/views_widget.patch	2026-08-20 16:03:41.000000000 +0200
+++ cef-7977/patch/patches/views_widget.patch	2026-09-16 19:42:56.671972611 +0200
@@ -61,10 +61,10 @@
      case ui::mojom::WindowShowState::kMaximized:
        return kSerializedShowStateMaximized;
 diff --git content/browser/renderer_host/render_widget_host_view_base.cc content/browser/renderer_host/render_widget_host_view_base.cc
-index 90bcd2280566727fde58edc93c370f2af20ca0ef..796221b15d6ae553858c7bc7208e747b57128a34 100644
+index e636ae0c6205c03414d562775194deabb7760ad8..481f7a1826104ae9626a6a62d5e733c4b57491aa 100644
 --- content/browser/renderer_host/render_widget_host_view_base.cc
 +++ content/browser/renderer_host/render_widget_host_view_base.cc
-@@ -725,6 +725,14 @@ float RenderWidgetHostViewBase::GetScaleOverrideForCapture() const {
+@@ -711,6 +711,14 @@ float RenderWidgetHostViewBase::GetScaleOverrideForCapture() const {
    return scale_override_for_capture_;
  }
  
@@ -80,7 +80,7 @@
    if (!GetMouseWheelPhaseHandler())
      return;
 diff --git content/browser/renderer_host/render_widget_host_view_base.h content/browser/renderer_host/render_widget_host_view_base.h
-index 39aa17737a96018776a97f831415f6b7ca10c395..43069e8190704f51286e8dd4472e5a58aed7c67a 100644
+index fbaad741af816c489092915a2fd856dcc5eee91e..1c6a52f4201060e58e319bd4633cd2cd30709aef 100644
 --- content/browser/renderer_host/render_widget_host_view_base.h
 +++ content/browser/renderer_host/render_widget_host_view_base.h
 @@ -90,6 +90,7 @@ namespace content {
@@ -98,10 +98,10 @@
 +  void SetHasExternalParent(bool val) override;
 +  bool HasExternalParent() const override;
 +
-   // Identical to `CopyFromSurface()`, except that this method issues the
-   // `viz::CopyOutputRequest` against the exact `viz::Surface` currently
-   // embedded by this View, while `CopyFromSurface()` may return a copy of any
-@@ -281,6 +285,10 @@ class CONTENT_EXPORT RenderWidgetHostViewBase
+   // For testing only.
+   virtual ui::FilteredGestureProvider* GetFilteredGestureProviderForTesting();
+ 
+@@ -266,6 +270,10 @@ class CONTENT_EXPORT RenderWidgetHostViewBase
    // Called when screen information or native widget bounds change.
    virtual void UpdateScreenInfo();
  
@@ -112,7 +112,7 @@
    // Called by the TextInputManager to notify the view about being removed from
    // the list of registered views, i.e., TextInputManager is no longer tracking
    // TextInputState from this view. The RWHV should reset |text_input_manager_|
-@@ -407,6 +415,12 @@ class CONTENT_EXPORT RenderWidgetHostViewBase
+@@ -393,6 +401,12 @@ class CONTENT_EXPORT RenderWidgetHostViewBase
                             const gfx::Rect& bounds,
                             const gfx::Rect& anchor_rect) = 0;
  
@@ -125,7 +125,7 @@
    // Indicates whether the page has finished loading.
    virtual void SetIsLoading(bool is_loading) = 0;
  
-@@ -734,6 +748,10 @@ class CONTENT_EXPORT RenderWidgetHostViewBase
+@@ -722,6 +736,10 @@ class CONTENT_EXPORT RenderWidgetHostViewBase
    // to all displays.
    gfx::Size system_cursor_size_;
  
@@ -136,7 +136,7 @@
   private:
    FRIEND_TEST_ALL_PREFIXES(
        BrowserSideFlingBrowserTest,
-@@ -755,10 +773,6 @@ class CONTENT_EXPORT RenderWidgetHostViewBase
+@@ -743,10 +761,6 @@ class CONTENT_EXPORT RenderWidgetHostViewBase
  
    void SynchronizeVisualProperties();
  
@@ -196,7 +196,7 @@
    if (host_ && set_focus_on_mouse_down_or_key_event_) {
      set_focus_on_mouse_down_or_key_event_ = false;
 diff --git content/public/browser/render_widget_host_view.h content/public/browser/render_widget_host_view.h
-index 673cb3358be84b0fe16b3c134cc19b77842ed366..0de25164e11ea9307e46d521e12c4528ae279e37 100644
+index 6b7a3301cb50e43d9d0ad983603750bd86886f72..ef51e6fe076d32ea23121ac5b38f82c8b68e1568 100644
 --- content/public/browser/render_widget_host_view.h
 +++ content/public/browser/render_widget_host_view.h
 @@ -273,6 +273,14 @@ class CONTENT_EXPORT RenderWidgetHostView {
@@ -215,10 +215,10 @@
    // Set the view's active state (i.e., tint state of controls).
    virtual void SetActive(bool active) = 0;
 diff --git third_party/blink/renderer/core/frame/web_frame_widget_impl.cc third_party/blink/renderer/core/frame/web_frame_widget_impl.cc
-index 92114bd98e2480cf88abb8838d16b3bcd27a3a4b..69b6fadeb3c2140194d3530e9b3d78a178d47f0e 100644
+index bcb63b49c8be96af7ec2f4b19cbfbb341f44e48b..8c792293cf79ce7f1296622135d44be4c2e64a12 100644
 --- third_party/blink/renderer/core/frame/web_frame_widget_impl.cc
 +++ third_party/blink/renderer/core/frame/web_frame_widget_impl.cc
-@@ -6030,6 +6030,7 @@ void WebFrameWidgetImpl::OnWindowShowStateChanged(
+@@ -6100,6 +6100,7 @@ void WebFrameWidgetImpl::OnWindowShowStateChanged(
        break;
      case WindowShowState::kInactive:
      case WindowShowState::kFullscreen:
@@ -258,10 +258,10 @@
 +  [MinVersion=1] kEnd = 7,
  };
 diff --git ui/ozone/platform/x11/x11_window.cc ui/ozone/platform/x11/x11_window.cc
-index 3605885b576cadf63b5db90cf46bb335c886d57a..92020fff92de1a1755475026fbbe8bec2bf03ed1 100644
+index de7873f39561d047db7023933c18cedd7d5d9a8f..cfe5ce81b04ecbe03ff5ba4567d1b2a09bad2942 100644
 --- ui/ozone/platform/x11/x11_window.cc
 +++ ui/ozone/platform/x11/x11_window.cc
-@@ -1935,7 +1935,17 @@ void X11Window::CreateXWindow(const PlatformWindowInitProperties& properties) {
+@@ -1936,7 +1936,17 @@ void X11Window::CreateXWindow(const PlatformWindowInitProperties& properties) {
    req.border_pixel = 0;
  
    last_set_bounds_px_ = SanitizeBounds(bounds);
@@ -294,10 +294,10 @@
    return host ? host->GetAcceleratedWidget() : nullptr;
  }
 diff --git ui/views/widget/desktop_aura/desktop_window_tree_host_linux.cc ui/views/widget/desktop_aura/desktop_window_tree_host_linux.cc
-index 1bd2dcc9302e09676d46c80e0706f3cc3ade47b3..262a2aab9fe7bd82eb8d8df305c80d3a8e7bc80c 100644
+index 3b6e7c0b55b8cfdbad42237dfd7082e520976bb3..2b750b7991131e12c1100b17a3fba2be946ff122 100644
 --- ui/views/widget/desktop_aura/desktop_window_tree_host_linux.cc
 +++ ui/views/widget/desktop_aura/desktop_window_tree_host_linux.cc
-@@ -30,6 +30,7 @@
+@@ -31,6 +31,7 @@
  #include "ui/linux/linux_ui.h"
  #include "ui/linux/window_frame_provider.h"
  #include "ui/native_theme/native_theme.h"
@@ -305,7 +305,7 @@
  #include "ui/platform_window/extensions/wayland_extension.h"
  #include "ui/platform_window/extensions/x11_extension.h"
  #include "ui/platform_window/platform_window.h"
-@@ -350,6 +351,18 @@ Widget::MoveLoopResult DesktopWindowTreeHostLinux::RunMoveLoop(
+@@ -373,6 +374,18 @@ Widget::MoveLoopResult DesktopWindowTreeHostLinux::RunMoveLoop(
    return result;
  }
  
@@ -324,7 +324,7 @@
  void DesktopWindowTreeHostLinux::DispatchEvent(ui::Event* event) {
    // In Windows, the native events sent to chrome are separated into client
    // and non-client versions of events, which we record on our LocatedEvent
-@@ -491,6 +504,15 @@ void DesktopWindowTreeHostLinux::AddAdditionalInitProperties(
+@@ -514,6 +527,15 @@ void DesktopWindowTreeHostLinux::AddAdditionalInitProperties(
    properties->wayland_app_id = params.wayland_app_id;
    properties->startup_id = params.startup_id;
  
@@ -341,7 +341,7 @@
    properties->x11_extension_delegate = this;
  
 diff --git ui/views/widget/desktop_aura/desktop_window_tree_host_linux.h ui/views/widget/desktop_aura/desktop_window_tree_host_linux.h
-index d440180f99339d3d9da1468ad0e5bf07a141c220..f4f7b69b2d843b8caf6edaef5312fe461f99e64f 100644
+index 73e98a4aa21bfe514287e5138909de11a98ffe15..2ae913e294c579a20d97780523f24a981d49e890 100644
 --- ui/views/widget/desktop_aura/desktop_window_tree_host_linux.h
 +++ ui/views/widget/desktop_aura/desktop_window_tree_host_linux.h
 @@ -70,6 +70,8 @@ class VIEWS_EXPORT DesktopWindowTreeHostLinux
@@ -353,7 +353,7 @@
   protected:
    // Overridden from DesktopWindowTreeHost:
    void Init(const Widget::InitParams& params) override;
-@@ -79,6 +81,8 @@ class VIEWS_EXPORT DesktopWindowTreeHostLinux
+@@ -81,6 +83,8 @@ class VIEWS_EXPORT DesktopWindowTreeHostLinux
        const gfx::Vector2d& drag_offset,
        Widget::MoveLoopSource source,
        Widget::MoveLoopEscapeBehavior escape_behavior) override;
@@ -362,7 +362,7 @@
  
    // PlatformWindowDelegate:
    gfx::Insets CalculateInsetsInDIP(
-@@ -146,6 +150,9 @@ class VIEWS_EXPORT DesktopWindowTreeHostLinux
+@@ -148,6 +152,9 @@ class VIEWS_EXPORT DesktopWindowTreeHostLinux
    base::ScopedObservation<ui::NativeTheme, ui::NativeThemeObserver>
        theme_observation_{this};
  
@@ -388,10 +388,10 @@
  
    // Calculate initial bounds.
 diff --git ui/views/widget/desktop_aura/desktop_window_tree_host_win.cc ui/views/widget/desktop_aura/desktop_window_tree_host_win.cc
-index 26a7efd6b9a2feabf3229a8dde05066345b90e1e..cce646097c052bba31e92f56860f7c99f2efdcd9 100644
+index 634640338e2951d852d9f5df5f7f0e48d97d00e8..2da5feee19ae8fc5ad2bd3798c0fbdd58500708c 100644
 --- ui/views/widget/desktop_aura/desktop_window_tree_host_win.cc
 +++ ui/views/widget/desktop_aura/desktop_window_tree_host_win.cc
-@@ -28,6 +28,7 @@
+@@ -30,6 +30,7 @@
  #include "ui/aura/client/aura_constants.h"
  #include "ui/aura/client/cursor_client.h"
  #include "ui/aura/client/focus_client.h"
@@ -399,7 +399,7 @@
  #include "ui/aura/window_event_dispatcher.h"
  #include "ui/base/class_property.h"
  #include "ui/base/cursor/cursor.h"
-@@ -203,17 +204,30 @@ void DesktopWindowTreeHostWin::Init(const Widget::InitParams& params) {
+@@ -261,7 +262,10 @@ void DesktopWindowTreeHostWin::Init(const Widget::InitParams& params) {
                          native_widget_delegate_.get());
  
    HWND parent_hwnd = nullptr;
@@ -409,8 +409,9 @@
 +    has_external_parent_ = true;
 +  } else if (params.parent && params.parent->GetHost()) {
      parent_hwnd = params.parent->GetHost()->GetAcceleratedWidget();
-   }
- 
+   } else if (params.context && params.context->GetHost() &&
+              params.type != Widget::InitParams::TYPE_WINDOW &&
+@@ -274,10 +278,20 @@ void DesktopWindowTreeHostWin::Init(const Widget::InitParams& params) {
    remove_standard_frame_ = params.remove_standard_frame;
    has_non_client_view_ = Widget::RequiresNonClientView(params.type);
    z_order_ = params.EffectiveZOrderLevel();
@@ -434,7 +435,7 @@
    message_handler_->Init(parent_hwnd, pixel_bounds);
  
    if (ShouldAddDWMBackdrop()) {
-@@ -238,6 +252,13 @@ void DesktopWindowTreeHostWin::Init(const Widget::InitParams& params) {
+@@ -302,6 +316,13 @@ void DesktopWindowTreeHostWin::Init(const Widget::InitParams& params) {
    InitHost();
    window()->Show();
  
@@ -448,7 +449,7 @@
    // Stack immediately above its parent so that it does not cover other
    // root-level windows, with the exception of menus, to allow them to be
    // displayed on top of other windows.
-@@ -1121,6 +1142,16 @@ void DesktopWindowTreeHostWin::HandleCreate() {
+@@ -1189,6 +1210,16 @@ void DesktopWindowTreeHostWin::HandleCreate() {
  }
  
  void DesktopWindowTreeHostWin::HandleDestroying() {
@@ -465,7 +466,7 @@
    drag_drop_client_->OnNativeWidgetDestroying(GetHWND());
    if (native_widget_delegate_) {
      native_widget_delegate_->OnNativeWidgetDestroying();
-@@ -1226,6 +1257,18 @@ void DesktopWindowTreeHostWin::HandleWindowMinimizedOrRestored(bool restored) {
+@@ -1294,6 +1325,18 @@ void DesktopWindowTreeHostWin::HandleWindowMinimizedOrRestored(bool restored) {
  
    if (restored) {
      window()->Show();
@@ -484,7 +485,7 @@
    } else {
      window()->Hide();
    }
-@@ -1247,11 +1290,17 @@ void DesktopWindowTreeHostWin::HandleFrameChanged() {
+@@ -1315,11 +1358,17 @@ void DesktopWindowTreeHostWin::HandleFrameChanged() {
  }
  
  void DesktopWindowTreeHostWin::HandleNativeFocus(HWND last_focused_window) {
@@ -504,7 +505,7 @@
  }
  
  bool DesktopWindowTreeHostWin::HandleMouseEvent(ui::MouseEvent* event) {
-@@ -1260,6 +1309,12 @@ bool DesktopWindowTreeHostWin::HandleMouseEvent(ui::MouseEvent* event) {
+@@ -1328,6 +1377,12 @@ bool DesktopWindowTreeHostWin::HandleMouseEvent(ui::MouseEvent* event) {
      return true;
    }
  
@@ -517,7 +518,7 @@
    SendEventToSink(event);
    return event->handled();
  }
-@@ -1455,9 +1510,18 @@ void DesktopWindowTreeHostWin::SetBoundsInDIP(const gfx::Rect& bounds) {
+@@ -1534,9 +1589,18 @@ void DesktopWindowTreeHostWin::SetBoundsInDIP(const gfx::Rect& bounds) {
    // positions in variable-DPI situations. See https://crbug.com/1224715 for
    // details.
    aura::Window* root = nullptr;
@@ -577,10 +578,10 @@
        break;
      case ui::mojom::WindowShowState::kEnd:
 diff --git ui/views/widget/widget.cc ui/views/widget/widget.cc
-index edb9eb307d53fae9cb8853de2a9deee06ecb4545..8873603850fea9acb990949f56bb489b0dcc6f86 100644
+index 2574bc2da3a76201a17c59110e58d135623abe7b..c15ee197c4219d66e1f68bbfdbd95ec1104b85c6 100644
 --- ui/views/widget/widget.cc
 +++ ui/views/widget/widget.cc
-@@ -244,8 +244,8 @@ ui::ZOrderLevel Widget::InitParams::EffectiveZOrderLevel() const {
+@@ -268,8 +268,8 @@ ui::ZOrderLevel Widget::InitParams::EffectiveZOrderLevel() const {
    }
  }
  
@@ -591,7 +592,7 @@
  }
  
  void Widget::InitParams::SetParent(gfx::NativeView parent_view) {
-@@ -506,7 +506,8 @@ void Widget::Init(InitParams params) {
+@@ -534,7 +534,8 @@ void Widget::Init(InitParams params) {
    }
  
    params.child |= (params.type == InitParams::TYPE_CONTROL);
@@ -601,7 +602,7 @@
    is_autosized_ = params.autosize;
  
    if (params.opacity == views::Widget::InitParams::WindowOpacity::kInferred &&
-@@ -607,9 +608,14 @@ void Widget::Init(InitParams params) {
+@@ -635,9 +636,14 @@ void Widget::Init(InitParams params) {
  
      if (show_state == ui::mojom::WindowShowState::kMaximized) {
        Maximize();
@@ -616,7 +617,7 @@
      }
  
  #if BUILDFLAG(IS_CHROMEOS)
-@@ -622,7 +628,12 @@ void Widget::Init(InitParams params) {
+@@ -650,7 +656,12 @@ void Widget::Init(InitParams params) {
    } else if (delegate) {
      SetContentsView(delegate->TransferOwnershipOfContentsView());
      if (should_set_initial_bounds) {
@@ -630,7 +631,7 @@
      }
    }
  
-@@ -2101,10 +2112,16 @@ void Widget::OnNativeWidgetParentChanged(gfx::NativeView parent) {
+@@ -2170,10 +2181,16 @@ void Widget::OnNativeWidgetParentChanged(gfx::NativeView parent) {
  }
  
  gfx::Size Widget::GetMinimumSize() const {
@@ -647,7 +648,7 @@
    return non_client_view_ ? non_client_view_->GetMaximumSize() : gfx::Size();
  }
  
-@@ -2410,7 +2427,8 @@ bool Widget::SetInitialFocus(ui::mojom::WindowShowState show_state) {
+@@ -2479,7 +2496,8 @@ bool Widget::SetInitialFocus(ui::mojom::WindowShowState show_state) {
    View* v = widget_delegate_->GetInitiallyFocusedView();
    if (!focus_on_creation_ ||
        show_state == ui::mojom::WindowShowState::kInactive ||
@@ -658,10 +659,10 @@
      // focus when the window is restored.
      if (v) {
 diff --git ui/views/widget/widget.h ui/views/widget/widget.h
-index 27656ce25c55291eb2c7e173b19bc1e06a9d41b1..eef1c81cda31a84943d8515f1f8e344684380907 100644
+index 4b71f0122b45668b0e3c0be11c1b0ef909273e4d..156b5371ab7c8c59946b068ec0fd196349ab3e7a 100644
 --- ui/views/widget/widget.h
 +++ ui/views/widget/widget.h
-@@ -442,6 +442,8 @@ class VIEWS_EXPORT Widget : public internal::NativeWidgetDelegate,
+@@ -445,6 +445,8 @@ class VIEWS_EXPORT Widget : public internal::NativeWidgetDelegate,
      // setters above.
      gfx::NativeView parent = gfx::NativeView();
  
@@ -670,7 +671,7 @@
      // Specifies the initial bounds of the Widget. Default is empty, which means
      // the NativeWidget may specify a default size. If the parent is specified
      // and the widget type is not WINDOW_TYPE_POPUP, `bounds` is in the parent's
-@@ -958,7 +960,7 @@ class VIEWS_EXPORT Widget : public internal::NativeWidgetDelegate,
+@@ -971,7 +973,7 @@ class VIEWS_EXPORT Widget : public internal::NativeWidgetDelegate,
    void SetVisible(bool visible);
  
    // Activates the widget, assuming it already exists and is visible.
@@ -680,10 +681,10 @@
    // Deactivates the widget, making the next window in the Z order the active
    // window.
 diff --git ui/views/widget/widget_delegate.h ui/views/widget/widget_delegate.h
-index 0581c9653c1f0d2c704e2d7e71f57c1149008912..09edcc73d6ef583676ad79a107e5b408e199357e 100644
+index fa5a0e60bcafc43f46b191f05080a2bbc51722ba..1fc281e20b324a8236b02771876cc5026b3ebc6c 100644
 --- ui/views/widget/widget_delegate.h
 +++ ui/views/widget/widget_delegate.h
-@@ -777,6 +777,10 @@ class VIEWS_EXPORT WidgetDelegate {
+@@ -773,6 +773,10 @@ class VIEWS_EXPORT WidgetDelegate {
    // Returns true if the title text should be centered.
    bool ShouldCenterWindowTitleText() const;
  
@@ -709,10 +710,10 @@
  
        if (native_widget_delegate->IsDialogBox()) {
 diff --git ui/views/win/hwnd_message_handler.cc ui/views/win/hwnd_message_handler.cc
-index 019f0d2a3e7241fb7131ce9a62b0b3c1e08e3a4e..bee5fd529d736a6cfa278d68d46a8c091b1b8cee 100644
+index 42fd0087319f0ce9a15b327b52403607e08d092a..fed80bd5d23de0a1fdb707ed236bc7b839718bf2 100644
 --- ui/views/win/hwnd_message_handler.cc
 +++ ui/views/win/hwnd_message_handler.cc
-@@ -907,7 +907,11 @@ bool HWNDMessageHandler::IsVisible() const {
+@@ -916,7 +916,11 @@ bool HWNDMessageHandler::IsVisible() const {
  }
  
  bool HWNDMessageHandler::IsActive() const {
@@ -725,7 +726,7 @@
  }
  
  bool HWNDMessageHandler::IsMinimized() const {
-@@ -3522,10 +3526,13 @@ LRESULT HWNDMessageHandler::HandleMouseEventInternal(UINT message,
+@@ -3483,10 +3487,13 @@ LRESULT HWNDMessageHandler::HandleMouseEventInternal(UINT message,
    } else if (event.type() == ui::EventType::kMousewheel) {
      ui::MouseWheelEvent mouse_wheel_event(msg);
      // Reroute the mouse wheel to the window under the pointer if applicable.
diff -urN cef-7977-orig/patch/patches/win_app_icon_3606.patch cef-7977/patch/patches/win_app_icon_3606.patch
--- cef-7977-orig/patch/patches/win_app_icon_3606.patch	2026-08-20 16:03:41.000000000 +0200
+++ cef-7977/patch/patches/win_app_icon_3606.patch	2026-09-16 19:42:56.676972289 +0200
@@ -88,29 +88,27 @@
  HICON GetAppIcon();
  HICON GetSmallAppIcon();
  
-diff --git chrome/common/chrome_constants.cc chrome/common/chrome_constants.cc
-index 6e30ef11474c457acf1bca690c6a310afb11d751..69121ac0ebfc800d505665da95cc63e703b8f247 100644
---- chrome/common/chrome_constants.cc
-+++ chrome/common/chrome_constants.cc
-@@ -5,6 +5,7 @@
- #include "chrome/common/chrome_constants.h"
+diff --git chrome/common/chrome_constants.h chrome/common/chrome_constants.h
+index 464a435a50d51f6a3d100c74645d8b48e010c1cc..f9cea6ee23ccbe10a1c1a26dc149102ec523d492 100644
+--- chrome/common/chrome_constants.h
++++ chrome/common/chrome_constants.h
+@@ -9,6 +9,7 @@
  
+ #include "base/files/file_path.h"
  #include "build/build_config.h"
 +#include "cef/libcef/features/features.h"
- #include "chrome/common/chrome_version.h"
  
- #define FPL FILE_PATH_LITERAL
-@@ -88,7 +89,12 @@ const char kMacHelperSuffixAlerts[] = " (Alerts)";
- #endif  // BUILDFLAG(IS_MAC)
+ namespace chrome {
+ 
+@@ -36,7 +37,11 @@ extern const char kMacHelperSuffixAlerts[];
  
  #if BUILDFLAG(IS_WIN)
--const base::FilePath::CharType kBrowserResourcesDll[] = FPL("chrome.dll");
-+const base::FilePath::CharType kBrowserResourcesDll[] =
+ inline constexpr base::FilePath::CharType kBrowserResourcesDll[] =
 +#if BUILDFLAG(ENABLE_CEF)
-+    FPL("libcef.dll");
++    FILE_PATH_LITERAL("libcef.dll");
 +#else
-+    FPL("chrome.dll");
+     FILE_PATH_LITERAL("chrome.dll");
 +#endif
- const base::FilePath::CharType kElfDll[] = FPL("chrome_elf.dll");
- const base::FilePath::CharType kStatusTrayWindowClass[] =
-     FPL("Chrome_StatusTrayWindow");
+ inline constexpr base::FilePath::CharType kElfDll[] =
+     FILE_PATH_LITERAL("chrome_elf.dll");
+ inline constexpr base::FilePath::CharType kRendererDll[] =


--- cef-7977-orig/patch/patches/chrome_browser_webui_license.patch	2026-08-20 16:03:41.000000000 +0200
+++ cef-7977/patch/patches/chrome_browser_webui_license.patch	2026-09-17 15:13:10.589023395 +0200
@@ -116,7 +116,7 @@
  #include "chrome/common/buildflags.h"
  #include "components/enterprise/buildflags/buildflags.h"
  #include "components/signin/public/base/signin_buildflags.h"
-@@ -196,6 +197,10 @@ inline constexpr char kChromeUIInternalsHost[] = "internals";
+@@ -207,6 +208,10 @@ inline constexpr char kChromeUIInternalsHost[] = "internals";
  inline constexpr char kChromeUIInterstitialHost[] = "interstitials";
  inline constexpr char kChromeUIInterstitialURL[] = "chrome://interstitials/";
  inline constexpr char kChromeUIIwaDevHost[] = "iwa-dev";
@@ -124,6 +124,6 @@
 +inline constexpr char kChromeUILicenseHost[] = "license";
 +inline constexpr char kChromeUILicenseURL[] = "chrome://license/";
 +#endif
- inline constexpr char kChromeUILocalStateHost[] = "local-state";
- inline constexpr char kChromeUILocalStateURL[] = "chrome://local-state";
- inline constexpr char kChromeUILocationInternalsHost[] = "location-internals";
+ inline constexpr char kChromeUILensOverlayHost[] = "lens-overlay";
+ inline constexpr char kChromeUILensOverlayUntrustedURL[] =
+     "chrome-untrusted://lens-overlay/";

--- cef-7977-orig/patch/patches/renderer_host_aura.patch	2026-08-20 16:03:41.000000000 +0200
+++ cef-7977/patch/patches/renderer_host_aura.patch	2026-09-19 02:30:35.560846622 +0200
@@ -1,5 +1,5 @@
 diff --git content/browser/renderer_host/render_widget_host_view_aura.cc content/browser/renderer_host/render_widget_host_view_aura.cc
-index 9aff51cf64b7decdd822cb582e160a69a04c6f3b..94912c146d178fc5fb8b52c7be683d0bab307ca6 100644
+index 443976e1b6ad4d1b2e86b2a886ed39b354f71393..d6e601b6df0734816cc26ad038c3c56d73967a2c 100644
 --- content/browser/renderer_host/render_widget_host_view_aura.cc
 +++ content/browser/renderer_host/render_widget_host_view_aura.cc
 @@ -8,6 +8,7 @@
@@ -18,17 +18,17 @@
  #include "content/public/common/page_visibility_state.h"
  #include "services/service_manager/public/cpp/interface_provider.h"
  #include "third_party/blink/public/common/input/web_input_event.h"
-@@ -859,6 +861,9 @@ gfx::Rect RenderWidgetHostViewAura::GetViewBoundsWithoutTransform() {
+@@ -868,6 +870,9 @@ gfx::Rect RenderWidgetHostViewAura::GetViewBoundsWithoutTransform() {
  void RenderWidgetHostViewAura::UpdateBackgroundColor() {
    CHECK(GetBackgroundColor());
  
 +  if (!window_) {
 +    return;
 +  }
-   SkColor color = *GetBackgroundColor();
-   window_->layer()->AsSolidColor()->SetColor(SkColor4f::FromColor(color));
- }
-@@ -1242,6 +1247,12 @@ void RenderWidgetHostViewAura::TransformPointToRootSurface(gfx::PointF* point) {
+   SkColor4f background_color =
+       SkColor4f::FromColor(GetBackgroundColor().value());
+   window_->layer()->SetFillsBoundsOpaquely(background_color.isOpaque());
+@@ -1253,6 +1258,12 @@ void RenderWidgetHostViewAura::TransformPointToRootSurface(gfx::PointF* point) {
  }
  
  gfx::Rect RenderWidgetHostViewAura::GetBoundsInScreen() {
@@ -41,9 +41,9 @@
    aura::Window* top_level = window_->GetToplevelWindow();
    gfx::Rect bounds(top_level->GetBoundsInScreen());
  
-@@ -2951,6 +2962,16 @@ void RenderWidgetHostViewAura::CreateAuraWindow(aura::client::WindowType type) {
-   window_->layer()->AsSolidColor()->SetColor(SkColor4f::FromColor(
-       GetBackgroundColor() ? *GetBackgroundColor() : SK_ColorWHITE));
+@@ -2986,6 +2997,16 @@ void RenderWidgetHostViewAura::CreateAuraWindow(aura::client::WindowType type) {
+   window_->layer()->SetFillsBoundsOpaquely(background_color.isOpaque());
+   window_->layer()->AsSurface()->SetFallbackBackgroundColor(background_color);
    UpdateFrameSinkIdRegistration();
 +
 +  // Do this after |window_| is created to avoid crashes on Win10.
@@ -59,10 +59,10 @@
  
  void RenderWidgetHostViewAura::UpdateFrameSinkIdRegistration() {
 diff --git content/browser/renderer_host/render_widget_host_view_aura.h content/browser/renderer_host/render_widget_host_view_aura.h
-index 345d1c40972ddecfd5668004ce4f12da36a7b4a6..41067acf0e3a6dff8763f393d97706a47d69fdc0 100644
+index 21422e7e2d44aa4766d081da2e42e7ba5ebd82f4..ffea7823346063f5307927377987df84c18e95ed 100644
 --- content/browser/renderer_host/render_widget_host_view_aura.h
 +++ content/browser/renderer_host/render_widget_host_view_aura.h
-@@ -480,6 +480,12 @@ class CONTENT_EXPORT RenderWidgetHostViewAura
+@@ -482,6 +482,12 @@ class CONTENT_EXPORT RenderWidgetHostViewAura
  
    bool ShouldUseDefaultDeadlineOnResize() const;
  
@@ -75,7 +75,7 @@
   protected:
    ~RenderWidgetHostViewAura() override;
  
-@@ -906,6 +912,8 @@ class CONTENT_EXPORT RenderWidgetHostViewAura
+@@ -909,6 +915,8 @@ class CONTENT_EXPORT RenderWidgetHostViewAura
  
    bool use_default_deadline_on_resize_ = false;
  
