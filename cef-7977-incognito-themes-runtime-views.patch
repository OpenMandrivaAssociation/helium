--- a/patch/patches/chrome_runtime_views.patch
+++ b/patch/patches/chrome_runtime_views.patch
@@ -663,13 +663,15 @@
    }
    GetRootView()->InvalidateLayout();
  }
-@@ -567,5 +602,8 @@ bool BrowserWidget::RegenerateFrameOnThemeChange(
+@@ -589,7 +624,10 @@ bool BrowserWidget::RegenerateFrameOnThemeChange(
  }
  
  bool BrowserWidget::IsIncognitoBrowser() const {
 +  if (!browser_view_) {
 +    return true;
 +  }
+   if (base::CommandLine::ForCurrentProcess()->HasSwitch("enable-incognito-themes"))
+     return false;
    return browser_view_->browser()->GetProfile()->IsIncognitoProfile();
  }
 diff --git chrome/browser/ui/views/frame/browser_widget.h chrome/browser/ui/views/frame/browser_widget.h
